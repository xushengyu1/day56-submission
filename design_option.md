# AI 失物招领匹配与认领复核系统 · 技术方案选型

**文档版本：** V3.0（用户确认最终选型）
**基线状态：** 基于 PRD V0.6 + 用户确认的技术选型
**更新日期：** 2026-07-16
**固定约束**：Python + FastAPI + PostgreSQL + pgvector（已在 PRD 中确认，不可更改）

> 本文档为方案比较阶段的顶层文档，包含顶层方案对比、关键子决策选型、最终选择及拒绝理由。
> 详细内容见 `docs/` 目录下的 8 份结构化文档。

---

## 一、方案取舍

### 1.1 顶层方案对比

| 方案 | 核心思路 | 前端 | AI 策略 | 认证 | 加权总分 |
|---|---|---|---|---|---:|
| **A：向量匹配 + LLM 核验（已选）** | pgvector 做双方向量化匹配取 TOP-5，LLM 自动生成隐藏问题并核验 | React SPA | text-embedding-v4 + mimoV2.5-pro | JWT | **4.25** |
| B：规则优先 + 最小 AI | 确定性规则做匹配，LLM 仅做隐藏回答核验 | SSR + Jinja2 | 规则匹配 + LLM 核验 | Session-Cookie | 3.90 |
| C：LLM 驱动全流程 | 匹配、核验、质量检查全部依赖 LLM API | React SPA | 全 LLM API | JWT | 3.40 |

> **红线说明**：三个方案在核心架构思路上有根本性区别：
> - 方案 A：向量召回 + LLM 生成问题与核验（向量 + LLM 协作）
> - 方案 B：规则为主、AI 为辅（确定性优先）
> - 方案 C：AI 为主、系统为辅（LLM 优先）
>
> 不是同一方案的低/中/高配。

---

### 1.2 方案 A 详细说明（✅ 已选）

**核心架构**：向量匹配 + LLM 生成问题与核验

| 层级 | 选择 | 理由 |
|---|---|---|
| 前端 | React 18 + Vite | 组件化、热重载、交互体验最佳；前后端分离 |
| 数据访问 | SQLAlchemy ORM + raw SQL for pgvector | ORM 加速 CRUD，pgvector 查询用 raw SQL 保证性能 |
| 认证 | JWT (access + refresh) | 前后端分离标准方案，无状态 |
| Embedding | `text-embedding-v4` | 双方信息均向量化，存入 pgvector |
| 匹配引擎 | pgvector 余弦相似度 | 失物与招领向量比较，取 TOP-5 候选 |
| 隐藏问题生成 | mimoV2.5-pro | 拾得者上传隐藏特征 → LLM 自动生成开放式问题 |
| 回答核验 | mimoV2.5-pro | 比对认领人回答与拾得者隐藏特征，语义判断 |

**核心业务流程**：

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. 发布阶段                                                      │
│                                                                  │
│   拾得者 ──→ 填写招领信息（类别/描述/时间/地点）                    │
│          ──→ 上传隐藏特征（私密描述，如"伞套内侧写有SZY"）          │
│          ──→ 上传整体图片                                         │
│                                                                  │
│   失主 ──→ 填写失物信息（类别/描述/时间/地点）                      │
│                                                                  │
│   系统 ──→ text-embedding-v4 将双方公开描述向量化                   │
│         ──→ 向量存入 PostgreSQL pgvector                           │
│                                                                  │
├─────────────────────────────────────────────────────────────────┤
│ 2. 匹配阶段                                                      │
│                                                                  │
│   失主发布失物后 ──→ 系统自动触发匹配                              │
│                                                                  │
│   系统 ──→ pgvector 余弦相似度计算（失物向量 vs 所有招领向量）       │
│         ──→ 结合时间/地点规则评分                                  │
│         ──→ 取综合分 TOP-5 候选返回                                │
│                                                                  │
├─────────────────────────────────────────────────────────────────┤
│ 3. 认领阶段                                                      │
│                                                                  │
│   失主 ──→ 查看 TOP-5 候选列表（展示匹配分/匹配点/冲突点）          │
│         ──→ 选择要认领的物品                                       │
│                                                                  │
│   系统 ──→ 读取该招领记录的隐藏特征                                │
│         ──→ mimoV2.5-pro 根据隐藏特征自动生成开放式验证问题          │
│         ──→ 向失主展示问题（不泄露答案）                            │
│                                                                  │
│   失主 ──→ 回答验证问题                                            │
│                                                                  │
│   系统 ──→ mimoV2.5-pro 比对回答 vs 隐藏特征                       │
│         ──→ 输出匹配/部分匹配/冲突/无法判断                         │
│         ──→ 计算隐藏核验分                                         │
│                                                                  │
├─────────────────────────────────────────────────────────────────┤
│ 4. 路由阶段                                                      │
│                                                                  │
│   高可信（候选分≥80 + 核验分≥85 + 无冲突）──→ 自动待交接            │
│   其它 ──→ 管理员复核                                             │
│                                                                  │
│   待交接 ──→ 展示联系方式 ──→ 线下交接 ──→ 拾得者标记已认领         │
└─────────────────────────────────────────────────────────────────┘
```

**与 PRD V0.6 的差异说明**：

| PRD 原设计 | 当前方案 | 差异原因 |
|---|---|---|
| 拾得者手动设置隐藏问题和标准答案（FR-21） | 拾得者只上传隐藏特征，LLM 自动生成问题 | 降低拾得者设置问题的门槛；LLM 可生成更规范的开放式问题 |
| AI 只引导和检查问题质量（FR-22） | AI 直接生成问题 | 拾得者不需要学习如何编写有效问题 |
| 至少两组有效问答才可发布（FR-23） | 拾得者上传隐藏特征即可发布，问题在认领时按需生成 | 简化发布流程；问题生成与认领绑定 |
| 前端待 `design_option.md` 确认 | React SPA | 已确认 |

**失败路径**：

| 失败场景 | 兜底策略 |
|---|---|
| text-embedding-v4 API 超时 | 降级为关键词匹配，标记"AI 不可用，使用规则匹配" |
| mimoV2.5-pro 问题生成超时 | 使用预置问题模板（"请描述物品的特殊标记"） |
| mimoV2.5-pro 核验超时 | 核验分按"无法判断"处理，转管理员复核 |
| pgvector 扩展不可用 | 降级为纯规则时间/地点/类别匹配 |
| JWT token 过期 | 前端自动刷新 refresh token |

---

### 1.3 方案 B 详细说明（已拒绝）

**核心架构**：规则优先 + 最小 AI

| 层级 | 选择 | 理由 |
|---|---|---|
| 前端 | Jinja2 SSR + 原生 HTML/JS/CSS | 零构建工具，2 天聚焦业务 |
| 数据访问 | SQLAlchemy ORM + raw SQL for pgvector | ORM 加速 CRUD |
| 认证 | Session-Cookie + bcrypt | SSR 友好 |
| Embedding | OpenAI `text-embedding-3-small` | 质量最高 |
| 核验 AI | OpenAI GPT-4o-mini | 结构化输出 |
| 问题质量 | 预置正则规则 | 零 API 调用 |

**拒绝原因**：

1. **前端交互体验一般**：表单提交 + 整页刷新，无实时反馈
2. **隐藏问题由拾得者手动编写**：学习成本高，问题质量参差不齐
3. **Embedding 模型与用户选择不符**：用户确认使用 text-embedding-v4

---

### 1.4 方案 C 详细说明（已拒绝）

**核心架构**：LLM 驱动全流程

| 层级 | 选择 | 理由 |
|---|---|---|
| 前端 | React 18 + Vite + TailwindCSS | 组件化、交互最佳 |
| 数据访问 | asyncpg 直接 SQL | 异步原生 |
| 认证 | JWT (access + refresh) | 前后端分离 |
| 候选匹配 | LLM 直接比较两段描述 | 语义最深 |
| 核验 AI | LLM | 全 LLM |

**拒绝原因**：

1. **匹配延迟过高**：每次匹配需调用 LLM，20 条记录对比 = 多次 API 调用
2. **LLM 输出不稳定**：同一输入多次调用可能不同分数，难以断言测试
3. **外部依赖最重**：匹配 + 核验 + 质量检查三处 LLM 调用

---

## 二、最终选择

**选择方案 A：向量匹配 + LLM 核验**

### 确认的技术选型

| 组件 | 选型 | 状态 |
|---|---|---|
| Web 框架 | FastAPI | ✅ PRD 已确认 |
| 前端框架 | **React 18 + Vite** | ✅ 用户确认 |
| 数据库 | PostgreSQL + pgvector | ✅ PRD 已确认 |
| ORM | SQLAlchemy + raw SQL for pgvector | ✅ 方案确认 |
| 认证 | JWT (access + refresh) | ✅ 方案确认（React SPA 需前后端分离） |
| Embedding 模型 | **text-embedding-v4** | ✅ 用户确认 |
| 匹配引擎 | pgvector 余弦相似度 → TOP-5 | ✅ 用户确认 |
| 隐藏问题生成 | **mimoV2.5-pro** | ✅ 用户确认 |
| 回答核验 AI | **mimoV2.5-pro** | ✅ 用户确认 |
| 图片存储 | MVP 本地文件目录 | ✅ 仅供人工查看 |

### 选择理由

| 维度 | 说明 |
|---|---|
| 匹配质量 | text-embedding-v4 双方信息均向量化，pgvector 余弦相似度召回 TOP-5，语义匹配能力最强 |
| 问题生成 | mimoV2.5-pro 根据隐藏特征自动生成规范的开放式问题，降低拾得者门槛 |
| 核验准确 | mimoV2.5-pro 语义比对，能识别同义表达和模糊描述 |
| 前端体验 | React SPA 组件化交互，候选展示/问答/状态管理体验最佳 |
| 架构清晰 | 前后端分离，各层职责明确 |

### 取舍代价（已接受）

| 取舍 | 代价 | 缓解措施 |
|---|---|---|
| 外部 API 依赖 | text-embedding-v4 + mimoV2.5-pro 均需网络 | 预置 fallback；Embedding 超时降级为关键词；核验超时转管理员 |
| React 开发成本 | 前端构建 + JWT + CORS 联调 | Vite 热重载加速开发；JWT 标准方案成熟 |
| LLM 问题生成不稳定 | 同一特征可能生成不同问题 | 问题生成有标准 prompt 模板；结果缓存 |
| 拾得者控制力降低 | 问题由 LLM 生成而非手动编写 | 拾得者可查看生成的问题并确认；上传隐藏特征的质量是关键 |

---

## 三、关键子决策选型

### D1：Embedding 模型 — text-embedding-v4

**选择理由**：用户确认。text-embedding-v4 支持多语言，向量维度适中，中文语义理解质量高。

**使用方式**：
- 失主发布失物 → 公开描述向量化 → 存入 pgvector
- 拾得者发布招领 → 公开描述向量化 → 存入 pgvector
- 匹配时 → pgvector 余弦相似度计算 → 结合时间/地点规则 → 取 TOP-5

### D2：核验 AI — mimoV2.5-pro

**选择理由**：用户确认。mimoV2.5-pro 语义理解能力强，支持中文，可生成结构化输出。

**使用方式**：
1. **问题生成**：输入拾得者隐藏特征 → mimoV2.5-pro 生成开放式验证问题
2. **回答核验**：输入拾得者隐藏特征 + 认领人回答 → mimoV2.5-pro 输出匹配/部分匹配/冲突/无法判断

### D3：前端框架 — React 18 + Vite

**选择理由**：用户确认。React SPA 提供最佳交互体验，Vite 热重载加速开发。

**技术细节**：
- React 18 + Vite + TailwindCSS
- 前后端通过 REST API (JSON) 通信
- JWT 认证，前端管理 token 状态

### D4：认证方案 — JWT

**选择理由**：React SPA 前后端分离，JWT 是标准方案。

**技术细节**：
- Access Token（短期，15 分钟）+ Refresh Token（长期，7 天）
- 前端 Axios 拦截器自动刷新
- 后端 FastAPI `python-jose` 库签发验证

### D5：匹配引擎 — pgvector 余弦相似度 + 规则评分

**选择理由**：pgvector 已确认；余弦相似度是最常用的向量匹配度量。

**评分公式**：

| 维度 | 权重 | 实现方式 |
|---|---:|---|
| 公开描述语义相似度 | 50 | pgvector 余弦相似度 × 50 |
| 时间接近度 | 20 | 规则：1h 内=20，1-4h=12，同天=6，跨日=0 |
| 地点接近度 | 20 | 规则：同地=20，同楼=14，邻区=8，无关=0 |
| 信息完整度 | 10 | 规则：关键字段完整度 |
| **合计** | **100** | 取 TOP-5 返回 |

---

## 四、技术选型总览

| 组件 | 选型 | 理由 |
|---|---|---|
| Web 框架 | FastAPI | REST API + 接口文档 + 结构化响应 |
| 前端 | **React 18 + Vite** | 组件化交互，用户体验最佳 |
| 数据库 | PostgreSQL + pgvector | 结构化数据 + 向量存储 |
| ORM | SQLAlchemy + raw SQL | CRUD 效率 + pgvector 灵活性 |
| 认证 | JWT (access + refresh) | 前后端分离标准方案 |
| Embedding | **text-embedding-v4** | 双方信息向量化，中文语义理解强 |
| 匹配引擎 | pgvector 余弦相似度 | 向量匹配 → TOP-5 |
| 隐藏问题生成 | **mimoV2.5-pro** | 根据隐藏特征自动生成开放式问题 |
| 回答核验 | **mimoV2.5-pro** | 语义比对，识别同义表达 |
| 图片存储 | MVP 本地文件目录 | 仅供人工查看，不进入 AI |

---

## 五、关联文档索引

| 文档 | 路径 | 说明 |
|---|---|---|
| PRD 基线 | `prd.md` | 需求基线 V0.6 |
| 问题诊断 | `docs/diagnosis/problem-diagnosis.md` | 问题全貌、核心冲突、约束条件 |
| 澄清问题 | `docs/diagnosis/clarifying-questions.md` | 澄清问题及回答 |
| 方案选项 | `docs/options/solution-options.md` | 3 个本质不同的方案详细描述 |
| 取舍矩阵 | `docs/options/tradeoff-matrix.md` | 多维度加权评分 |
| 拒绝方案 | `docs/options/rejected-options.md` | 被拒绝方案及原因 |
| 验证案例 | `docs/validation/cases-and-results.md` | 验证场景（待执行） |
| 最终建议 | `docs/decision/final-recommendation.md` | 最终选择与实施计划 |
| 决策备忘 | `docs/decision/decision-memo.md` | ADR 标准格式决策记录 |

---

## 六、决策链路状态

```text
问题诊断 ✅ → 澄清问题 ✅ → PRD Review ✅ → 方案对比 ✅
→ MVP 选择 ✅ → 验证计划 ✅ → 最终建议 ✅ → 决策备忘 ✅
```

---

> **文档版本**：v3.0
> **修订日期**：2026-07-16
> **适用范围**：AI 失物招领匹配与认领复核系统 Web 版
> **与 PRD V0.6 差异**：隐藏问题由 LLM 自动生成（PRD 原设计为拾得者手动设置）
