# 最终建议

**文档版本：** V3.0（用户确认最终选型）
**更新日期：** 2026-07-16
**决策状态：** ✅ 用户已确认

---

## 最终选择

**选择方案 A：向量匹配 + LLM 核验**

在 Python + FastAPI + PostgreSQL + pgvector 已确认的前提下，用户确认采用：

- **前端**：React 18 + Vite
- **数据访问**：SQLAlchemy ORM + raw SQL for pgvector
- **认证**：JWT (access + refresh)
- **Embedding**：text-embedding-v4（双方信息均向量化）
- **匹配引擎**：pgvector 余弦相似度 + 时间/地点规则评分 → TOP-5
- **隐藏问题生成**：mimoV2.5-pro（拾得者上传隐藏特征 → LLM 自动生成问题）
- **回答核验**：mimoV2.5-pro（语义比对回答与隐藏特征）

---

## 选择理由

### 核心判断：向量匹配 + LLM 核验是最优组合

1. **匹配质量最高**：text-embedding-v4 双方信息均向量化，pgvector 余弦相似度语义召回 TOP-5
2. **拾得者门槛最低**：只需上传隐藏特征，LLM 自动生成规范的开放式问题
3. **核验准确**：mimoV2.5-pro 语义比对，能识别同义表达和模糊描述
4. **前端体验最佳**：React SPA 组件化交互，候选展示/问答/状态管理体验最佳
5. **架构清晰**：前后端分离，各层职责明确

### 为什么不选方案 B（规则优先 + 最小 AI）

- 前端交互体验差（SSR + 整页刷新）
- 拾得者手动编写问题学习成本高、质量参差不齐
- Embedding 和核验模型与用户选择不符

### 为什么不选方案 C（LLM 驱动全流程）

- 匹配延迟过高（每次匹配需调用 LLM）
- LLM 输出不稳定，难以断言测试
- 无法利用已确认的 pgvector 能力

---

## 取舍代价（已接受）

| 取舍 | 代价 | 缓解措施 |
|---|---|---|
| 外部 API 依赖 | text-embedding-v4 + mimoV2.5-pro 均需网络 | Embedding 超时降级为关键词；核验超时转管理员 |
| React 开发成本 | Vite + JWT + CORS 联调 | Vite 热重载加速；JWT 标准方案成熟 |
| LLM 问题生成不稳定 | 同一特征可能生成不同问题 | 标准 prompt 模板；拾得者确认机制 |
| 拾得者控制力降低 | 问题由 LLM 生成 | 拾得者可查看并确认生成的问题 |

---

## 实施计划

### 第一天

| 时段 | 任务 | 负责 |
|---|---|---|
| 上午 | PostgreSQL 建表 + pgvector 扩展 + SQLAlchemy ORM 模型 | 成员 A |
| 上午 | FastAPI 框架 + JWT 认证中间件 | 成员 A |
| 上午 | text-embedding-v4 API 集成 + 向量生成服务 | 成员 B |
| 下午 | 失物/招领记录 CRUD API + 向量化入库 | 成员 A |
| 下午 | pgvector 匹配引擎（余弦相似度 + 时间/地点规则 → TOP-5） | 成员 B |
| 下午 | React 前端：项目搭建 + 注册登录 + 发布页面 | 成员 C |
| 晚上 | 候选列表/详情页面 + 接口联调 | 成员 B + C |

### 第二天

| 时段 | 任务 | 负责 |
|---|---|---|
| 上午 | mimoV2.5-pro 隐藏问题生成服务 | 成员 B |
| 上午 | mimoV2.5-pro 回答核验服务 | 成员 B |
| 上午 | 自动/人工路由逻辑 + 管理员复核页面 | 成员 A |
| 下午 | 联系方式授权 + 交接确认 + 审计日志 | 成员 A |
| 下午 | React 前端联调 + 测试用例执行 | 成员 C |
| 晚上 | E2E 验证 + 演示准备 + 文档收尾 | 全员 |

---

## 风险与缓解

| 风险 | 概率 | 影响 | 缓解措施 |
|---|---:|---|---|
| text-embedding-v4 API 不可用 | 中 | 高 | 预置一组向量结果作为 fallback；降级为关键词匹配 |
| mimoV2.5-pro API 不可用 | 中 | 高 | 预置一组核验结果作为 fallback；降级为规则匹配转管理员 |
| mimoV2.5-pro 问题生成质量差 | 中 | 中 | 设计标准 prompt 模板；拾得者确认机制 |
| pgvector 安装/配置问题 | 低 | 高 | 提前本地验证；准备 Docker 镜像 |
| React + JWT 联调时间不足 | 中 | 中 | 核心页面优先；管理员页面可简化 |
| 阈值不合理 | 中 | 中 | 用边界样例验证；答辩时说明 Demo 局限 |
