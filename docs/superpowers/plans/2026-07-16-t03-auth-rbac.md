# T03 认证与 RBAC 实施计划

> 遵循每个模块先写失败测试、再最小实现、再回归验证的节奏。

**Goal:** 完成注册、登录、refresh、Bearer 依赖和角色/资源归属授权。

## Task 1：安全原语

- [x] 写 `tests/unit/auth/test_security.py` 的 Red 测试
- [x] 实现 `auth/security.py` 和 JWT 配置
- [x] 运行安全单元测试并记录边界

## Task 2：RBAC 与 DTO

- [x] 写 RBAC/schema 单元测试
- [x] 实现 `auth/schemas.py`、`auth/rbac.py`、`api/deps.py`
- [x] 运行 focused tests

## Task 3：认证服务与路由

- [x] 写注册/登录/refresh 集成测试
- [x] 实现 `auth/service.py`、`api/routes/auth.py` 并注册路由
- [x] 运行真实 PostgreSQL 集成测试

## Task 4：回归、证据、提交

- [x] 更新 `evidence/development-records/T03.md`
- [x] 运行 T03 focused、全量 pytest、compileall、diff check
- [x] 提交 `feat(auth): add jwt sessions and resource level authorization` 并推送
