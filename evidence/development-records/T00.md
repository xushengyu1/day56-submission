# T00 后端工程骨架与健康检查证据

## 范围

- 任务：`task-backend.md` T00
- 分支：`day56-development`
- 实现范围：`src/backend` FastAPI 入口、配置、存活检查和数据库就绪检查
- 未包含：认证、模型、迁移、图片、AI、前端和真实 PostgreSQL

## Red

命令：

```bash
cd src/backend
../../.venv/bin/pytest tests/unit/test_health.py -q
```

结果：失败（收集阶段），错误为 `ModuleNotFoundError: No module named 'app'`。此时 T00 应用包尚不存在，失败原因是目标功能缺失。

## Green 与回归

命令：

```bash
cd src/backend
../../.venv/bin/pytest tests/unit/test_health.py -q
```

结果：`3 passed in 0.39s`

命令：

```bash
cd src/backend
../../.venv/bin/pytest -q
```

结果：`3 passed in 0.31s`

## 功能验证

使用 FastAPI `TestClient` 替换 `check_database` 为成功检查，验证以下请求均返回 `{"status": "ok"}`：

- `GET /api/health/live`
- `GET /api/health/ready`
- `GET /ready`

结果：输出 `T00 functional check passed`。

单元测试另验证数据库检查抛出异常时，`/ready` 返回 HTTP 503 和 `{"status": "unavailable"}`，响应不包含异常文本。

## 环境

- Python：3.12.13（满足 T00 的 Python 3.11+ 约束）
- 本地实际包版本：FastAPI 0.139.0、httpx 0.28.1、pydantic-settings 2.14.2、pytest 9.1.1、SQLAlchemy 2.0.51
- `pyproject.toml` 声明的目标版本遵循 `dev.md`；本次测试使用已有虚拟环境，没有重新安装依赖。
- 未启动真实 PostgreSQL；T00 测试通过替换数据库检查函数完成。

## 结论

T00 的 Red-Green、单元回归和功能验证均已执行。T01 数据模型与迁移尚未开始。
