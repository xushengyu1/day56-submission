# T02 核验规则、评分与状态机证据

## 范围

- 任务：`task-backend.md` T02
- 分支：`day56-development`
- 交付：身份证规范化/HMAC/掩码、OTHER 隐藏问题校验、候选评分与 Top 5 排序、记录与认领状态机
- 边界：纯函数实现，不包含数据库服务、JWT、HTTP 路由或 AI provider 调用

## 设计与 Red

设计和执行计划分别记录在：

- `docs/superpowers/specs/2026-07-16-t02-core-rules-design.md`
- `docs/superpowers/plans/2026-07-16-t02-core-rules.md`

每个模块先写测试。首次运行时，四个新模块均因尚未创建而在收集阶段失败：

```text
ModuleNotFoundError: app.verification.identity
ModuleNotFoundError: app.verification.other
ModuleNotFoundError: app.matching.scoring
ModuleNotFoundError: app.items.state_machine
```

测试覆盖了身份证校验位/日期/全角空白/脱敏、HMAC key 约束、问题数量/开放式/维度/答案泄漏（题目和公开描述）、50/20/20/10 评分边界、硬门槛、Top 5 稳定排序，以及记录/认领允许和禁止迁移。

## Green 与回归

执行环境：`Python 3.12.13`（项目目标版本为 3.11+）。

```bash
cd src/backend
../../.venv/bin/pytest tests/unit/verification tests/unit/matching tests/unit/items -q
```

结果：`51 passed`。

```bash
../../.venv/bin/pytest -q
```

结果：`64 passed`（包含 T00/T01 回归）。

```bash
../../.venv/bin/python -m compileall -q app tests
git diff --check
```

结果：均退出 0。异常消息使用稳定字段名，不包含完整身份证号码、答案文本或其他原始敏感输入；测试输出未出现此类泄漏。

## 结论

T02 的四个纯函数模块已按 Red-Green 完成，单元测试与全量后端回归通过，具备进入后续认证、匹配和认领服务实现的稳定规则入口。
