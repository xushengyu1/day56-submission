# T04 审计与幂等证据

## 范围

- 任务：`task-backend.md` T04
- 分支：`day56-development`
- 交付：递归审计脱敏、事件投影、事务内追加、UTC 时钟、请求 ID、结构化日志、稳定 API 错误、同主体幂等结果
- 复用：T01 已建好的 `audit_events` 与 `idempotency_results` 表和 ActorType 枚举

## 设计与 Red

设计与执行计划：

- `docs/superpowers/specs/2026-07-16-t04-audit-idempotency-design.md`
- `docs/superpowers/plans/2026-07-16-t04-audit-idempotency.md`

先运行测试，模块未实现时收集阶段按预期失败，主要错误为：

```text
ModuleNotFoundError: No module named 'app.audit.schemas'
ModuleNotFoundError: No module named 'app.audit.projection'
ModuleNotFoundError: No module named 'app.api.errors'
```

## Green 与回归

```bash
cd src/backend
../../.venv/bin/pytest tests/unit/audit tests/unit/core tests/integration/audit -q
```

结果：`11 passed`。覆盖递归键名/值模式脱敏、投影追踪字段、请求哈希稳定性、审计提交/回滚、同主体同键重放、同键不同请求冲突及不同主体隔离。

```bash
../../.venv/bin/pytest -q
```

结果：`90 passed`（包含 T00–T03 回归）。

```bash
../../.venv/bin/python -m compileall -q app tests
git diff --check
```

结果：均退出 0。测试只使用合成身份证号码和答案；审计 metadata、幂等响应及日志字段均通过统一脱敏函数处理，未把完整号码、答案、token 或 PRIVATE 路径写入事件。

## 结论

T04 已完成可复用的审计与幂等基础设施。业务模块可在自己的事务中追加事件，并用 actor 与幂等键安全重放状态变更响应。
