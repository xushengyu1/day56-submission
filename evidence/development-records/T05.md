# T05 图片服务证据

## 范围

- 任务：`task-backend.md` T05
- 分支：`day56-development`
- 交付：JPEG/PNG/WebP MIME 与魔数校验、大小/像素限制、随机 object key、本地 PRIVATE/PUBLIC 隔离、Pillow 区域不可逆遮挡、PRIVATE 清理
- 依赖：`pillow==11.3.0`、`python-multipart==0.0.20`

## 设计与 Red

设计与执行计划：

- `docs/superpowers/specs/2026-07-16-t05-image-service-design.md`
- `docs/superpowers/plans/2026-07-16-t05-image-service.md`

初始测试先运行；环境未安装 Pillow 时先出现预期的 `ModuleNotFoundError: No module named 'PIL'`，随后模块尚未实现的接口收集失败。安装锁定依赖并实现后进入 Green。

## Green 与回归

```bash
cd src/backend
../../.venv/bin/pytest tests/unit/images tests/integration/images -q
```

结果：`7 passed`。单元覆盖真实魔数、MIME/大小/像素边界、路径穿越和遮挡像素；集成覆盖 PRIVATE 上传、PUBLIC 脱敏副本、数据库资产记录及关闭清理只删除 PRIVATE。

```bash
../../.venv/bin/pytest -q
```

结果：`97 passed`（包含 T00–T04 回归）。

```bash
../../.venv/bin/python -m compileall -q app tests
git diff --check
```

结果：均退出 0。API 返回只包含 asset ID/purpose，不返回文件系统路径；PUBLIC 目录只能由确认脱敏流程写入，测试未发现 PRIVATE 文件泄漏。

## 结论

T05 已建立可供 T07 发布流程复用的图片边界：客户端 MIME 不可信、object key 不可控、PRIVATE 与 PUBLIC 物理隔离，脱敏副本必须显式确认。
