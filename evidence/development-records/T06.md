# T06 AI 适配器与模型契约证据

## 范围

- 任务：`task-backend.md` T06
- 分支：`day56-development`
- 交付：供应商无关端口、Pydantic 输出 DTO、固定 mock、OpenAI 兼容 HTTP 适配器、确定性 hash embedding、Docker mock 服务
- 边界：模型输出是草稿/判断建议；非法 JSON、低置信或 HTTP 故障不自动改变业务状态

## 设计与 Red

设计与执行计划：

- `docs/superpowers/specs/2026-07-16-t06-ai-adapters-design.md`
- `docs/superpowers/plans/2026-07-16-t06-ai-adapters.md`

初始契约测试先运行，按预期出现 `app.multimodal.mock`、`app.matching.embedding` 和 `app.multimodal.openai_compatible` 未实现的收集错误。

## Green 与回归

```bash
cd src/backend
../../.venv/bin/pytest tests/contract tests/unit/multimodal -q
```

结果：`6 passed`。覆盖图片提取、2～3 个问题、保守回答核验、固定维度 embedding、OpenAI message JSON 解析、503 单次重试和非法 JSON 拒绝。

```bash
../../.venv/bin/pytest -q
```

结果：`103 passed`（包含 T00–T05 回归）。

## Docker 功能验证

```bash
docker compose up -d --build --wait ai-mock
curl -fsS http://127.0.0.1:18080/health
curl -fsS -H 'content-type: application/json' \
  -d '{"model":"mock","operation":"extract_found_item","input":{}}' \
  http://127.0.0.1:18080/v1/chat/completions
```

结果：`day56-development-ai-mock-1` 为 healthy；health 返回 `{"status":"ok"}`，提取接口返回固定合成雨伞 JSON。

```bash
../../.venv/bin/python -m compileall -q app tests
git diff --check
```

结果：均退出 0。适配器不暴露 API key、PRIVATE image ref 或未清洗的模型原始响应；完整号码候选没有进入 `raw_result_redacted`。

## 结论

T06 已建立真实/模拟模型的明确边界，后续 T07/T10 可注入相同端口并保持失败降级、版本追踪和结构校验一致。
