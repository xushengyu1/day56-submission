# T07 招领发布证据

## 范围

- 任务：`task-backend.md` T07
- 分支：`day56-development`
- 交付：FOUND 草稿、AI 建议、人工确认、身份证/OTHER 类型专属确认、脱敏、发布策略和 7 条 found-record API
- 复用：T02 身份证/问题规则与状态机、T04 审计、T05 图片隔离、T06 mock/embedding

## Red

三组集成测试先写并运行，初始收集阶段均因 `app.items.service` 尚不存在而失败。测试随后分别驱动草稿版本、身份证发布和 OTHER 发布的最小实现。

## Green 与回归

```bash
cd src/backend
../../.venv/bin/pytest \
  tests/integration/items/test_found_draft.py \
  tests/integration/items/test_found_identity_publish.py \
  tests/integration/items/test_found_other_publish.py -q
```

结果：`3 passed`。真实 PostgreSQL 验证了 owner/expected_version、原图门槛、身份证 HMAC/掩码和确认脱敏副本、OTHER 2 个已确认问题，以及发布状态/embedding/审计同事务写入。

```bash
../../.venv/bin/pytest -q
```

结果：`106 passed`（包含 T00–T06 回归）。

```bash
../../.venv/bin/python -m compileall -q app tests
git diff --check
```

结果：均退出 0。数据库只保存合成身份证的 HMAC ASCII 摘要与 `110***********0010` 掩码，完整号码未进入响应、审计或公开字段；问题答案只保存于 VERIFICATION 表。

## 结论

T07 已完成“AI 仅建议、人工确认后发布”的招领闭环。任何公共字段、原图、HMAC/脱敏或问题集缺失都会返回 `PUBLISH_GUARD_FAILED` 并保持 DRAFT。
