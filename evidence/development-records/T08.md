# T08 失物与候选证据

## 范围

- 任务：`task-backend.md` T08
- 分支：`day56-development`
- 交付：LOST 发布、公开文本 embedding、FOUND 硬过滤、50/20/20/10 评分、Top 5 快照、候选列表/详情 PUBLIC DTO

## Red

三组集成测试先写并运行，初始均因 `app.matching.service` 尚不存在而在收集阶段失败。随后按 query/scoring/privacy 顺序实现最小模块。

## Green 与回归

```bash
cd src/backend
../../.venv/bin/pytest tests/integration/matching -q
```

结果：`3 passed`。测试数据库包含 6 条同类型 FOUND，验证返回不超过 5 条、owner 检查、每条四项分数之和等于总分，以及相同输入稳定排序。

隐私测试对序列化 DTO 扫描，确认不包含 `embedding`、`event_time_exact`、`location_normalized`、`answer_key`、`hmac`、`object_key` 或 `contact`。

```bash
../../.venv/bin/pytest -q
```

结果：`109 passed`（包含 T00–T07 回归）。

```bash
../../.venv/bin/python -m compileall -q app tests
git diff --check
```

结果：均退出 0。

## 结论

T08 已完成失物发布到安全候选查询的闭环。候选快照保留模型/规则版本和输入 hash，普通用户只接收确认后的 PUBLIC 字段与固定分数理由。
