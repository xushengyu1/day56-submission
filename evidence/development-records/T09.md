# T09 身份证认领证据

## 范围

- 任务：`task-backend.md` T09
- 分支：`day56-development`
- 交付：候选 owner/type 校验、HMAC 精确匹配、最多 2 次尝试、candidate 行锁、重复证件人工路由、identity claim API

## Red

三组集成测试先运行，初始均因 `app.verification.service` 尚不存在而收集失败。随后按唯一匹配、并发限制、重复路由顺序实现。

## Green 与回归

```bash
cd src/backend
../../.venv/bin/pytest \
  tests/integration/verification/test_identity_claim.py \
  tests/integration/verification/test_identity_attempt_concurrency.py \
  tests/integration/verification/test_duplicate_identity_routing.py -q
```

结果：`3 passed`。

- 唯一正确 HMAC：claim、LOST、FOUND 进入 `PENDING_HANDOFF`。
- 两个并发错误请求：candidate 行锁串行化后仅产生 attempt 1/2，第二次为 `LOCKED`。
- 同 HMAC 两条活动 FOUND：进入 `PENDING_ADMIN_REVIEW`，不自动交接。

```bash
../../.venv/bin/pytest -q
```

结果：`112 passed`（包含 T00–T08 回归）。

```bash
../../.venv/bin/python -m compileall -q app tests
git diff --check
```

结果：均退出 0。ClaimAttempt 只保存 HMAC 和稳定 result code；响应/审计不包含完整号码、HMAC 或位差异。

## 结论

T09 已完成身份证认领的并发安全闭环，唯一正确匹配才可自动待交接，失败超限与重复身份均采取保守路由。
