# T10 OTHER 认领证据

## 范围

- 任务：`task-backend.md` T10
- 分支：`day56-development`
- 交付：问题安全投影、回答提交、模型核验、安全摘要、全匹配/低置信/异常保守路由

## Red

三组集成测试先运行，初始因 `get_other_questions` 与 `submit_other_claim` 尚不存在而收集失败。随后按问题 API、认领路由、模型失败顺序实现。

## Green 与回归

```bash
cd src/backend
../../.venv/bin/pytest \
  tests/integration/verification/test_other_questions_api.py \
  tests/integration/verification/test_other_claim_routing.py \
  tests/integration/verification/test_other_model_failure.py -q
```

结果：`3 passed`。

- 问题 DTO 仅含 id/text/dimension，不含答案键和隐藏描述。
- 两个关键答案全部匹配、置信度 0.95 且无其他活动认领时进入 `PENDING_HANDOFF`。
- adapter 抛 `MODEL_UNAVAILABLE` 时 claim 进入 `PENDING_ADMIN_REVIEW`，不自动通过。

ClaimAttempt 的 `answer_summary` 只包含 result/confidence/reason code，不保存原始回答文本。

```bash
../../.venv/bin/pytest -q
```

结果：`115 passed`（包含 T00–T09 回归）。

```bash
../../.venv/bin/python -m compileall -q app tests
git diff --check
```

结果：均退出 0。

## 结论

T10 已完成 OTHER 认领的安全闭环；只有结构有效、全部关键题匹配且无并发认领时自动待交接，其余情况统一保守转人工。
