# T11 管理员复核证据

## 范围

- 任务：`task-backend.md` T11
- 分支：`day56-development`
- 交付：UNMATCHED/CLAIM_REVIEW 主动复核、异常 claim 队列、管理员详情/审计查询、带理由幂等决定

## Red

三组集成测试先运行，初始均因 `app.reviews.service` 尚不存在而收集失败。随后按主动请求、队列投影、管理员决定实现。

## Green 与回归

```bash
cd src/backend
../../.venv/bin/pytest tests/integration/reviews -q
```

结果：`3 passed`。

- owner 可分别创建一条活动 UNMATCHED 和 CLAIM_REVIEW，重复活动请求返回 `ACTIVE_REVIEW_EXISTS`。
- ADMIN 可读取最小队列，USER 被拒绝；DTO 扫描无 HMAC、答案键、submitted_hmac、object key、密码或 token。
- 相同管理员/idempotency key/body 重放原结果，只写一条 AdminReview；批准状态为 `PENDING_HANDOFF`，不直接 CLAIMED。

```bash
../../.venv/bin/pytest -q
```

结果：`118 passed`（包含 T00–T10 回归）。

```bash
../../.venv/bin/python -m compileall -q app tests
git diff --check
```

结果：均退出 0。决定理由和固定结果进入审计，PRIVATE 原图与完整身份证没有管理员读取接口。

## 结论

T11 已完成主动复核到管理员决定的可追溯闭环，权限、最小投影、状态上限和幂等约束均由服务端执行。
