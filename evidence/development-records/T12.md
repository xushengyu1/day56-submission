# T12 交接与时间线证据

## 范围

- 任务：`task-backend.md` T12
- 分支：`day56-development`
- 交付：claim requester 最小联系方式、finder 幂等交接确认、相关用户/管理员时间线

## Red

先新增三组集成测试并运行：收集阶段分别因 `get_claim_contact`、`complete_handoff`、`get_record_timeline` 尚不存在而失败。候选快照审计中另补一条回归测试，旧实现会删除已被 claim 引用的 candidate，测试先失败为 `None is not None`。

## Green

```bash
cd src/backend
../../.venv/bin/pytest tests/integration/handoffs -q
```

结果：`3 passed`。

- 仅 `PENDING_HANDOFF` claim 的 requester 能读取 finder email；其他用户统一得到 `NOT_FOUND`。
- 仅对应 finder 可确认交接；claim、LOST、FOUND 在同一事务进入 `CLAIMED`，相同幂等键只生成一条 `HANDOFF_COMPLETED` 审计事件。
- record owner、相关 claim requester、ADMIN 可读取时间线；普通用户投影删除 admin/private/internal 元数据。
- OpenAPI 已注册三个 T12 接口；共 29 个 paths。
- 候选重算只清理未被 claim 引用的快照，交接链路中的历史 candidate 不被级联删除。

## 总验收

```bash
../../.venv/bin/pytest -q
../../.venv/bin/ruff check .
../../.venv/bin/mypy .
../../.venv/bin/python -m compileall -q app tests
git diff --check
```

结果：`122 passed`；Ruff 全部通过；Mypy 检查 150 个源文件无问题；compileall 与 diff check 退出 0。

`task-backend.md` 中 T00–T12 的 13 条聚焦验收命令再次逐项执行，结果依次为：`3、10、52、14、7、7、4、3、4、3、3、3、3 passed`。

```bash
../../.venv/bin/alembic check
../../.venv/bin/alembic downgrade base
../../.venv/bin/alembic upgrade head
../../.venv/bin/alembic check
../../.venv/bin/pytest -q
```

结果：升级前后均为 `No new upgrade operations detected`；六段迁移可完整降级和升级；干净 schema 上 `121 passed`，加入候选快照回归后最终为 `122 passed`。

```bash
docker compose up -d --wait postgres ai-mock
docker compose ps
curl --fail http://127.0.0.1:18080/health
```

结果：PostgreSQL 与 AI mock 均为 `healthy`，AI mock 返回 `{"status":"ok"}`。

## 隐私结论

生产代码扫描中没有将完整身份证号、答案键、隐藏描述、私有对象键或密码写入日志的调用。命中项仅存在于受控输入、PRIVATE/VERIFICATION 数据模型、HMAC 比对及经过 `redact_metadata` 的结构化日志路径；候选、复核、时间线 DTO 测试均验证敏感字段不出现在响应中。

## 结论

T12 已完成联系方式授权、幂等交接、状态闭环和角色化审计时间线；T00–T12 后端任务全部通过聚焦、全量、静态、迁移及运行态验收。
