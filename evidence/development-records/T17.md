# T17：隔离 E2E seed 与真实主流程

**执行日期：** 2026-07-17

**状态：** 已完成

**数据：** 仅合成账号、合成号码和程序生成 PNG

## 实现

- `src/backend/scripts/seed_e2e.py` 只接受 `APP_ENV=e2e` 且数据库名以 `_e2e` 结尾。
- seed 自动创建隔离数据库、升级 Alembic、清空应用表、写入合成 USER/ADMIN 并生成两张合成图片。
- `frontend/playwright.config.ts` 启动真实 FastAPI 与 Vite，强制单 worker，并使用真实 HTTP 和 PostgreSQL。
- `auth-home.spec.ts`、`matching-other.spec.ts`、`identity-admin.spec.ts` 覆盖认证、两类核验、主动复核、管理员决定和交接。

## 实际验证

```text
APP_ENV=e2e DATABASE_URL=postgresql+asyncpg://app:app@127.0.0.1:55432/lost_found_e2e npm run test:e2e
4 passed (15.3s)
```

其中 OTHER 场景完成类别/区域过滤、详细地点排序、隐藏问题、认领、联系方式和拾得者交接；身份证场景完成用户确认、脱敏、号码核验、失败锁定和管理员复核。

## 相关提交

- `05845db8 test(e2e): add isolated database seed`
- `6b405e08 test(e2e): add real auth browser harness`
- `6d620225 test(e2e): cover matching admin and security journeys`

## 限制

默认使用 mock AI 和 mock embedding，未将外部模型质量写成已验证事实。
