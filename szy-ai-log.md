# AI 协作日志 — SZY — AI 失物招领匹配与认领复核系统

> 本文件是我的个人 AI 协作记录，只筛选实际改变问题判断、产品边界、AI 边界、评分规则或阶段结论的对话。
> 每条记录严格包含：目的 / 输入 / 建议 / 人工判断 / 验证。
> **关键决策标记**：🔴 拒绝 AI 建议 / 🟡 修改 AI 建议 / 🟢 采纳但有附加条件。
> 当前状态：PRD V0.8、方案 A 与端到端详细设计 V1.3 已确认（React + FastAPI + PostgreSQL/pgvector + text-embedding-v4 + MiMo-V2.5）；已生成开发规范和 TDD 任务计划。代码、单元测试、集成测试和端到端测试尚未开始，文中不得把计划验证写成实际通过。

---

## 阶段一：作业理解与交付范围

### 记录 1：采用阶段门推进并保留全过程证据 🟢

| 字段 | 内容 |
|---|---|
| **目的** | 确定两天作业的实施流程以及过程文档如何留存。 |
| **输入** | 作业题目截图；我提出的完整流程：问题拆解 → PRD → PRD 复盘确认 → design_option → 方案确认 → design → Design Review → dev → TDD → 实现与测试 → 证据链 → 答辩；指定参考 `day5材料` 中的文件模板。 |
| **建议** | AI 建议不要直接编码，先按阶段门生成问题诊断、PRD、方案、设计、开发和验证材料；同时把每次澄清、采纳、修改和拒绝写入对应过程文档。 |
| **人工判断** | **采纳但增加硬约束**：我同意阶段门方式，但要求最终文件结构严格参考 `E:\A-xiaomi\xiaomi\2\day5\day5材料`，所有实际交互都必须留下记录，不能只生成结果文档，也不能事后伪造 AI 日志。 |
| **验证** | `day6` 已按模板建立诊断、协作、AI、方案和验证文档；当前阶段状态写入 `README.md`、`docs/collaboration/decision-log.md` 和 `docs/diagnosis/prd-iteration-log.md`。 |

### 记录 2：把交付重点从“功能数量”收缩为最小证据闭环 🟡

| 字段 | 内容 |
|---|---|
| **目的** | 明确两天内真正需要证明什么，避免做成大而全的失物招领平台。 |
| **输入** | 我补充的六项评分重点：问题判断、方案取舍、AI 边界、MVP 可验证、证据链可信、答辩能抗追问；以及双技术端、五类以上 AI 协作、失败场景、端到端测试等硬性要求。 |
| **建议** | AI 初版 PRD 包含较多页面、状态和外围能力；Review 后建议缩成一条“发布 → 候选 → 认领核验 → 自动或人工路由 → 交接 → 留痕”的完整主线。 |
| **人工判断** | **修改 AI 的初版范围**：我确认评分重点不是页面数量和视觉效果，而是一个可以运行、触发判断、展示失败并追溯证据的最小系统。复杂申诉、多组织、多渠道通知和生产级能力均不进入 MVP。 |
| **验证** | `prd.md` 第 4 章已写成 Goals / Non-Goals；第 6 章只保留核心主流程；`README.md` 明确真实实现、模拟数据、当前边界和未实现内容。运行结果仍需开发完成后验证。 |

---

## 阶段二：角色、保管责任与认领边界

### 记录 3：重新定义业务用户、管理员和实物保管责任 🟡

| 字段 | 内容 |
|---|---|
| **目的** | 消除 AI、管理员、失物招领处和实物保管人之间的角色混淆。 |
| **输入** | 我明确：业务用户只分失主和拾得者；AI 不是角色；不设置失物招领处；实物由拾得者保管；异常申请由统一管理员审核。 |
| **建议** | AI 建议把“业务用户”和“系统权限角色”分层：失主、拾得者是业务用户，管理员是审核角色，AI 是系统能力；管理员只检查系统证据，不接触和保管实物。 |
| **人工判断** | **采纳分层表达并修正初版角色模型**：我接受管理员作为审核权限角色，但拒绝把 AI、保管人或失物招领处写成独立用户角色。拾得者必须承担保管和最终交接确认责任。 |
| **验证** | `prd.md` 第 2、3、6 章分别给出用户画像、权限边界和三角色流程；流程中只有拾得者可以在线下交接后将物品标记为“已认领”。实现后需用越权测试验证。 |

### 记录 4：隐藏问题必须由拾得者手工设置 🟡

| 字段 | 内容 |
|---|---|
| **目的** | 明确隐藏核验信息的来源，以及拾得者未提供隐藏信息时如何处理。 |
| **输入** | 初版存在“AI 生成隐藏问题”的表述；我指出只有拾得者持有实物，失主信息只应用于初步候选匹配，并追问拾得者未填写隐藏信息怎么办。 |
| **建议** | AI 建议由拾得者手工填写问题和标准答案，AI 只提示可检查的部位、检查问题质量和答案泄露风险；若没有核验基准，不允许正式发布。AI 进一步建议至少设置 2 题，其中至少 1 题为关键问题。 |
| **人工判断** | **修改“AI 自动生成问题”的建议并采纳安全门槛**：我确认隐藏问题只能由拾得者根据实物手工设置，AI 不能编造标准答案。接受“至少两题且一题关键”的 MVP 门槛；不满足时只能保存草稿。 |
| **验证** | `prd.md` 7.3、7.6 和主流程 Mermaid 已包含草稿门槛、手工问答和 AI 质量检查。开发后需测试 0 题、1 题、无关键题和问题泄露答案四类失败场景。 |

### 记录 5：高可信只允许进入“待交接”，不能直接确认归属 🟡

| 字段 | 内容 |
|---|---|
| **目的** | 定义高置信度认领如何自动流转，以及管理员何时介入。 |
| **输入** | 我提出高置信度可以直接确认，只有需要复核的申请交给管理员；交接后由拾得者修改为“已认领”。 |
| **建议** | AI 指出公开候选高分只能证明两条记录相似，不能证明所有权；建议拆成候选匹配分、隐藏核验分和综合认领可信度，三者同时过线且无风险时仅自动进入“待交接”，而不是直接标记“已认领”。 |
| **人工判断** | **接受 AI 对我原方案的安全性修改**：自动流转条件定为候选分 ≥80、隐藏核验分 ≥85、综合可信度 ≥85、至少两题且关键题无冲突、无 AI 异常或试探风险；其它情况转管理员，不由 AI 自动拒绝。实际交接后仍只允许拾得者确认“已认领”。 |
| **验证** | `prd.md` 7.7.1 写明全部条件，8 章定义状态流转，13 章列出正向与反向验收场景。阈值只是 Demo 初始规则，仍需边界样例验证误放行和误转人工情况。 |

---

## 阶段三：AI 使用边界与产品表达

### 记录 6：拒绝使用图像识别和图片 AI 匹配 🔴

| 字段 | 内容 |
|---|---|
| **目的** | 决定拾得者上传整体图片后，是否使用视觉模型提取类别、颜色和物品特征。 |
| **输入** | 我分析了图像识别限制：同类黑伞外观接近、角度和光照影响大、小划痕或内部标记不可见、模型可能生成不存在的细节，而且公开图片可能泄露隐藏答案。 |
| **建议** | AI 初版架构曾包含根据图片提取特征或辅助匹配的可能；Review 后建议保留整体图片上传，但只供失主人工浏览，不进入任何 AI 评分。 |
| **人工判断** | **拒绝视觉 AI 路线**：我决定图片只作为人工发现候选的辅助材料，不调用图像识别、不生成图片向量、不提取隐藏特征，也不进入候选分或认领可信度。上传时必须提示拾得者遮挡敏感内容和隐藏答案。 |
| **验证** | `prd.md` 4.2 Non-Goals、5.4 图片分级、7.3 图片需求和 7.4.1 评分规则均排除图片 AI；后续需要检查接口、数据库和提示词中不存在图片特征或图片向量。 |

### 记录 7：从参考文字中只提取适合本项目的产品描述和画像 🟡

| 字段 | 内容 |
|---|---|
| **目的** | 补齐产品背景、解决的问题、核心价值、架构图、技术栈和用户画像。 |
| **输入** | 我上传的产品描述参考文字，并明确后端采用 Python + FastAPI、当时数据库采用 MySQL；要求只提取需要的部分。 |
| **建议** | AI 建议复用参考材料中“信息分散、描述差异、记忆偏差、防冒领、异常分流、证据追溯”等内容，同时生成需求级架构图和失主、拾得者、管理员画像；不照搬其中的 SQLite、图像识别、管理员保管实物等设定。 |
| **人工判断** | **采纳提炼方式但拒绝照搬参考方案**：我保留能解释当前 MVP 的背景和用户痛点，删除与已确认边界冲突的视觉 AI、失物招领处、AI 自动生成隐藏事实等内容。前端、ORM、认证和具体模型不在 PRD 阶段提前锁定。 |
| **验证** | `prd.md` 第 1 章包含产品描述、Mermaid 架构和技术栈，第 2 章包含三类画像；`docs/diagnosis/prd-iteration-log.md` 迭代 02 记录了采纳与拒绝内容。 |

---

## 阶段四：数据权限、数据库与流程表达

### 记录 8：把公开/隐藏两级改为四级数据权限模型 🟡

| 字段 | 内容 |
|---|---|
| **目的** | 解决联系方式被放入“公开信息”但又要求授权后显示的权限矛盾。 |
| **输入** | 我提出 `PUBLIC / MATCH_ONLY / VERIFICATION / PRIVATE` 四级含义、候选页/匹配服务/管理员的访问矩阵，以及地点、图片和联系方式的跨级示例。 |
| **建议** | AI 建议把四级定义转成可测试的服务端访问规则：候选页只返回 PUBLIC；匹配使用 PUBLIC + MATCH_ONLY；核验服务使用 VERIFICATION 但不回传答案；管理员按需查看前三类；PRIVATE 默认脱敏且不进入 AI。 |
| **人工判断** | **采纳四级模型并进一步限制 PRIVATE**：我确认联系方式和原始凭证不属于 PUBLIC，管理员也不能因为角色身份默认查看全部 PRIVATE。字段级别由字段值、用途和生命周期决定，而不是由字段名称固定决定。 |
| **验证** | `prd.md` 第 5 章已经包含权限矩阵、四级定义、同字段分级示例和访问规则。后续 TDD 必须覆盖普通用户响应字段扫描、管理员脱敏和联系方式授权前越权访问。 |

### 记录 9：把 MVP 范围前置为 Goals / Non-Goals 🟢

| 字段 | 内容 |
|---|---|
| **目的** | 避免产品目标和后置 MVP 范围重复，导致同一需求在两个章节中发生漂移。 |
| **输入** | 我提供了 Goals / Non-Goals 的参考格式，并要求写入 PRD 第 4 章、删除原第 12 章 MVP 范围、重新调整后续编号。 |
| **建议** | AI 建议合并重复范围，形成可验证的目标和明确的非目标，并将角色与权限放在第 3 章、Goals / Non-Goals 放在第 4 章。 |
| **人工判断** | **采纳结构调整但要求内容以本项目事实为准**：不照抄参考项目的 CLI、FAQ 和 mock LLM 内容，而是改写为本失物招领系统的双技术端、核心主线、失败场景、数据权限和 AI 边界。 |
| **验证** | `prd.md` 第 4 章包含 12 项 Goals、10 项 Non-Goals 和产品原则；原独立 MVP 范围已删除，顶层章节连续编号为 1～15。 |

### 记录 10：用 PostgreSQL + pgvector 替代 MySQL 🔴

| 字段 | 内容 |
|---|---|
| **目的** | 统一存储结构化业务数据和文本向量，降低两天 MVP 的服务数量。 |
| **输入** | 此前 PRD 已按我的旧决定写为 MySQL；随后我明确数据库改用 PostgreSQL，并要求同时保存结构化数据和向量数据。 |
| **建议** | AI 建议使用 PostgreSQL 常规表或 JSON 保存业务、状态和审计数据，启用 pgvector 保存公开文本 embedding；不额外部署 Milvus、Pinecone 等独立向量数据库。 |
| **人工判断** | **拒绝并替代此前 MySQL 决策**：我确认采用 PostgreSQL + pgvector。只保存 `PUBLIC + MATCH_ONLY` 文本向量；图片、VERIFICATION 和 PRIVATE 数据不进入候选向量。embedding 模型、维度、距离和索引留到方案阶段比较。 |
| **验证** | `prd.md` 架构、技术栈、数据对象和非目标已统一为 PostgreSQL + pgvector；历史 MySQL 记录保留并标注被替代。后续需要验证扩展初始化、向量维度、查询失败降级和权限隔离。 |

### 记录 11：核心业务流程统一改为 Mermaid 🟢

| 字段 | 内容 |
|---|---|
| **目的** | 让自动流转、管理员复核和三角色责任在评审与答辩中更直观。 |
| **输入** | 我要求把 `prd.md` 第 6 章中的流程图全部转换为 Mermaid。 |
| **建议** | AI 建议分别保留主流程、失主流程、拾得者流程和管理员流程四张图，在主流程中明确隐藏问答发布门槛、高可信分支、人工分支、交接和审计终点。 |
| **人工判断** | **采纳四图结构**：我没有把三个角色压缩成一张大图，因为分图更容易说明个人操作和管理员边界；同时要求业务语义保持不变。 |
| **验证** | `prd.md` 6.1～6.4 已包含 4 个 Mermaid 代码块，没有残留 text 流程图；已做源码结构检查。当前环境没有 Mermaid CLI，实际渲染仍需在后续文档构建或演示环境验证。 |

---

## 阶段五：候选评分复盘与 PRD 确认

### 记录 12：合并重复的外观分和公开描述语义分 🟡

| 字段 | 内容 |
|---|---|
| **目的** | 消除候选匹配标准对颜色、品牌和公开外观的重复计分。 |
| **输入** | PRD V0.5 原评分为：颜色与公开外观 25、时间 20、地点 20、公开描述语义 25、完整度 10；我指出颜色和公开外观本质上已经属于公开描述语义。 |
| **建议** | AI 建议删除独立外观维度，把公开描述语义提高为 50 分，保留时间 20、地点 20、完整度 10，总分仍为 100；类别继续作为硬门槛。向量相似度由 AI embedding + pgvector 实现。 |
| **人工判断** | **采纳合并方案并限定只计算一次证据**：颜色、品牌、名称和其它公开外观可以写入公开描述并参与向量语义匹配，但不得再单独加分；图片和隐藏信息仍不进入向量。 |
| **验证** | `prd.md` 7.4.1 已改为 50 + 20 + 20 + 10，字段说明、目标、测试计划和过程日志同步更新。后续必须验证“语义很像但时间或地点明显冲突”不会被 50 分语义权重错误放行。 |

### 记录 13：确认 PRD V0.6 为需求基线 🟢

| 字段 | 内容 |
|---|---|
| **目的** | 结束 PRD Review，建立后续方案设计可以依赖的稳定需求基线。 |
| **输入** | 完成角色、隐藏核验、图片边界、四级权限、PostgreSQL + pgvector、Mermaid 流程和候选评分五轮迭代后的 `prd.md` V0.6。 |
| **建议** | AI 建议把当前版本标记为已确认基线；确认动作本身不增加版本号，只有后续需求内容发生变化时才从 V0.7 起新增迭代；下一阶段进入 `design_option.md`，不能跳过方案比较直接编码。 |
| **人工判断** | **采纳版本治理建议**：我明确当前 PRD 已确认，后续如果有变更再迭代。V0.6 保留为已确认事实，不允许静默覆盖；方案、设计和实现仍按既定阶段门推进。 |
| **验证** | `prd.md` 头部已标记“V0.6 / 已确认（2026-07-16）”；确认事实同步到 `docs/validation/review-record.md`、`docs/collaboration/decision-log.md`、会议记录和 PRD 迭代记录。 |

---

## 阶段六：个人 AI 证据整理

### 记录 14：个人 AI 日志只保留完整决策链 🟢

| 字段 | 内容 |
|---|---|
| **目的** | 把本次 PRD 阶段中真正有判断价值的 AI 协作整理成我的个人贡献证据。 |
| **输入** | 我指定参考 `xiaomi/new-day3/ai-log.md`，并要求文件名为 `szy-ai-log.md`；每条必须包含目的、输入、建议、人工判断、验证，其中人工判断必须明确采纳、修改或拒绝了什么以及原因。 |
| **建议** | AI 建议按参考文件的阶段结构和红黄绿标记整理，只筛选改变产品范围、角色责任、AI 边界、数据权限、技术事实、评分规则或阶段状态的记录；普通文件操作不用于凑数，未执行测试不写成通过。 |
| **人工判断** | **采纳筛选与五字段格式，并强化真实性要求**：我不需要原始对话的机械转录，而需要能说明个人判断的决策链。所有记录以第一人称写清我的取舍；“验证”必须区分已存在的文档证据和未来需要执行的系统测试。 |
| **验证** | `szy-ai-log.md` 已生成；使用脚本检查每条记录的五字段数量一致。文件末尾另外列出拒绝/修改证据和当前验证边界，便于答辩追溯。 |

---

## 本阶段人工判断汇总

| 类型 | 数量 | 代表性内容 |
|---|---:|---|
| 🔴 拒绝 / 替代 | 2 | 拒绝图像识别与图片 AI 匹配；用 PostgreSQL + pgvector 替代 MySQL |
| 🟡 修改 AI 或原始方案 | 7 | 收缩 MVP、修正角色、手工隐藏问答、三分数安全门槛、参考内容筛选、四级权限、评分去重 |
| 🟢 附条件采纳 | 5 | 阶段门、Goals/Non-Goals、Mermaid 四图、PRD 基线治理、个人 AI 日志五字段规则 |

### 已形成的两条明确拒绝/明显修改证据

1. 我拒绝了图片视觉识别和图片向量匹配路线，整体图片只供人工查看。
2. 我替代了先前的 MySQL 决策，改用 PostgreSQL + pgvector，并限制只有公开文本可以进入候选向量。
3. 我修改了“高置信度直接确认”的原始想法：系统只能自动进入待交接，不能直接确认归属或已交接。
4. 我修改了原 25 分外观 + 25 分语义的重复评分，合并为公开描述向量语义 50 分。

---

## 阶段七：技术方案比较与选型

### 记录 15：方案比较文档结构与决策链路 🟢

| 字段 | 内容 |
|---|---|
| **目的** | 基于已确认的 PRD V0.6，生成 design_option.md 及 docs/ 下 8 份结构化决策文档 |
| **输入** | PRD V0.6（1147 行）、参考格式（new-day3/S3/03-design-options.md）、8 份文档清单（问题诊断/澄清问题/方案选项/取舍矩阵/拒绝方案/验证案例/最终建议/决策备忘） |
| **建议** | AI 按决策链路”问题诊断→澄清问题→方案对比→MVP 选择→验证→最终建议”一次性生成全部 9 份文档；提出 3 个方案：A.规则优先+最小AI（Jinja2 SSR）、B.LLM驱动全流程（React SPA）、C.混合分层+渐进增强（HTMX）；推荐方案 A（加权总分 4.10） |
| **人工判断** | **采纳文档结构和决策链路框架**。9 份文档的组织方式、决策链路的步骤顺序、取舍矩阵的评分维度（可交付性/证明力/运行风险/可解释性/前端体验/证据链/可维护性）均合理，直接采纳。但 AI 的方案推荐基于其保守假设，后续被我的具体选型覆盖 |
| **验证** | 9 份文档全部生成至 docs/ 目录，结构完整 |

### 记录 16：前端技术栈选择 🔴

| 字段 | 内容 |
|---|---|
| **目的** | 确定前端技术方案 |
| **输入** | AI 方案 A 推荐 Jinja2 SSR + 原生 HTML/JS/CSS，评分”两天可交付性 5/5”；认为 React SPA “2天交付风险最高”（加权总分 3.55 vs 方案A的4.10） |
| **建议** | AI 推荐 Jinja2 SSR，理由：零构建工具、2天聚焦业务、前后端同一进程无CORS、联调面最小 |
| **人工判断** | **拒绝 Jinja2 SSR，选择 React 18 + Vite**。理由：(1) React 组件化交互体验远优于表单提交+整页刷新；(2) Vite 热重载加速开发，实际开发效率不比 SSR 差太多；(3) SPA 前后端分离架构更清晰 |
| **验证** | design_option.md 前端更新为 React 18 + Vite，认证方案随之从 Session-Cookie 改为 JWT |

### 记录 17：Embedding 与核验模型选择 🔴

| 字段 | 内容 |
|---|---|
| **目的** | 确定文本向量化模型和语义核验模型 |
| **输入** | AI 推荐 OpenAI `text-embedding-3-small`（Embedding）和 OpenAI GPT-4o-mini（核验），理由：质量最高、API最简单、结构化输出 |
| **建议** | AI 在方案 A、D1、D2 子决策中均选择 OpenAI 系列模型 |
| **人工判断** | **拒绝 AI 推荐，用户直接指定模型**：Embedding 改为 text-embedding-v4，核验改为 mimoV2.5-pro。AI 的推荐基于通用场景最优，但用户有明确的模型偏好 |
| **验证** | design_option.md 和所有 docs/ 文档同步更新为 text-embedding-v4 + mimoV2.5-pro |

### 记录 18：隐藏问题生成方式的根本性变更 🔴

| 字段 | 内容 |
|---|---|
| **目的** | 确定认领时隐藏验证问题的来源 |
| **输入** | AI 完全沿用 PRD V0.6 的手动设置模式：拾得者手动设置隐藏问题和标准答案（FR-21），AI 只引导和检查质量（FR-22），至少两组有效问答才可发布（FR-23） |
| **建议** | AI 在所有方案中均未提出替代方案，完全沿用手动设置模式 |
| **人工判断** | **拒绝手动设置模式，改为 LLM 自动生成**。用户确认：拾得者只上传隐藏特征（私密文字描述如”伞套内侧写有SZY”），由 mimoV2.5-pro 根据隐藏特征自动生成开放式验证问题。理由：(1) 降低拾得者编写问题的门槛；(2) LLM 可生成更规范的开放式问题；(3) 简化发布流程 |
| **验证** | design_option.md 流程重写；PRD 从 V0.6 升级到 V0.7，FR-21/22/23 和相关章节全部修改 |

---

## 阶段八：PRD 同步升级

### 记录 19：PRD V0.6→V0.7 升级方式 🟡

| 字段 | 内容 |
|---|---|
| **目的** | 将 design_option.md 确认的技术选型回写到 PRD |
| **输入** | design_option.md V3.0 确认的选型（React/text-embedding-v4/mimoV2.5-pro/JWT/LLM生成问题）与 PRD V0.6 存在 4 处核心差异 |
| **建议** | AI 建议保留 V0.6 不动，另起 V0.7 文档 |
| **人工判断** | **修改为原地升级 PRD**。用户要求”以现在这个 design 为准”，直接在 prd.md 上修改升级为 V0.7。理由：design 是最终确认的方案，PRD 应与 design 保持一致，不应保留与实际方案矛盾的旧版本作为工作文档 |
| **验证** | prd.md 版本号更新为 V0.7，技术栈表/架构图/流程图/FR-21~23/数据模型/验收标准/用户画像/角色职责等全部同步 |

### 记录 20：PRD 一致性全局扫描 🔴

| 字段 | 内容 |
|---|---|
| **目的** | 验证 PRD V0.7 是否与 design_option.md 完全一致 |
| **输入** | AI 完成 PRD 修改后声称”已更新完毕” |
| **建议** | AI 未主动进行一致性验证 |
| **人工判断** | **要求 AI 进行完整性检验**。用户要求”检验一下是否更新完毕”，AI 启动后台 Agent 逐项比对 16 个检查点，发现 4 项 FAIL（架构图仍写”待方案确认”、4处残留”手动设置”、2处残留”至少两组有效隐藏问答”、拾得者痛点未更新），约 18 处旧设计语言残留 |
| **验证** | 修复后 16/16 检查项全部 PASS，全局 grep 扫描无残留旧术语（手动设置 0、至少两组 0、隐藏问答 0、待方案确认 0） |

### 记录 21：旧术语全量清理 🟡

| 字段 | 内容 |
|---|---|
| **目的** | 清理 PRD 中所有与新设计不一致的旧术语 |
| **输入** | Agent 全局扫描发现约 18 处残留：散布在 1.1.4/1.2/2.3/3.1/3.3/4.1/5.4/5.6/5.7/6.5/7.7.1/10/13.1 等章节 |
| **建议** | AI 在初次修改时只更新了 FR-21/22/23 和主流程图，遗漏了散布在其他章节的旧术语引用 |
| **人工判断** | **要求逐一定位并修复全部 18 处**。替换规则：手动设置→上传隐藏特征、至少两组→上传隐藏特征、隐藏问答→隐藏特征、拾得者预设→LLM 根据隐藏特征生成、架构图”待方案确认”→具体技术名。修复后 grep 扫描确认无残留 |
| **验证** | 全局扫描 0 残留；16/16 检查项 PASS |

---

## 阶段九：身份证件与多模态方案重构

### 记录 22：用受控多模态替代“图片不进 AI”旧边界 🟡

| 字段 | 内容 |
|---|---|
| **目的** | 让拾得者上传图片后自动得到物品名称和特征描述，同时控制识别错误和敏感图片泄露。 |
| **输入** | 我重新明确：物品分身份证件类和其他；拾得者填写时间地点、上传图片，多模态模型自动识别名称和特征，拾得者可修改；失主填写时间地点、物品和描述，可选上传图片。 |
| **建议** | AI 指出这与此前“图片只人工查看、不进入 AI”的确认边界冲突，建议把图片 AI 限定为发布辅助：PRIVATE 原图进入受控多模态提取，输出只作为草稿，拾得者修改确认后才生成公开文本和候选向量；失败时手工填写。 |
| **人工判断** | **修改并替代我此前拒绝视觉 AI 的决定**。我采纳多模态发布辅助，因为这是新的明确需求；同时接受 AI 的安全限制，不允许模型结果直接发布，也不做视觉相似度自动认领。失主可选图片暂只作为 PRIVATE 支持材料，是否自动识别待 PO 确认。 |
| **验证** | `prd.md` 升级为 V0.8；`design_option.md` V4 和问题诊断、澄清、方案、矩阵、验证文档均加入 AI 草稿/人工确认/失败降级。真实模型图片 spike 尚未执行。 |

### 记录 23：身份证件改用号码精确核验 🟡

| 字段 | 内容 |
|---|---|
| **目的** | 为身份证件类建立不依赖隐藏特征的认领规则，并避免公开证件号码。 |
| **输入** | 我明确：身份证件展示时对号码掩码；拾得者无需填写隐藏特征；失主填写证件号码，匹配成功即可认领。 |
| **建议** | AI 建议把“即可认领”解释为进入待交接而非直接标记已认领；完整号码不进入 LLM、embedding 或普通日志。服务端规范化号码后使用 HMAC 精确比较，候选只展示掩码和脱敏图片；错误不提示差异，尝试超限/多人/异常转管理员。 |
| **人工判断** | **采纳双核验分支并修改自动结果语义**：身份证件无需隐藏特征，号码精确匹配成功进入待交接；其他物品仍填写隐藏特征并走 LLM 核验。我接受掩码、HMAC 和防试探限制，因为完整号码属于敏感身份数据；最终已认领仍由拾得者交接后确认。 |
| **验证** | PRD 已增加 `IDENTITY_DOCUMENT / OTHER`、HMAC/掩码/原图与脱敏图、双 Mermaid 流程和验收用例；验证文档包含号码成功、错误、X 大小写、尝试超限、DTO/日志泄露等测试。代码尚未实现。 |

### 记录 24：旧 V3 方案不得静默沿用，重新比较三条路线 🟢

| 字段 | 内容 |
|---|---|
| **目的** | 在新需求下重做 design_option，并维护 8 份强制决策文档。 |
| **输入** | 我要求参考 `new-day3/S3/03-design-options.md`，严格按问题诊断 → ≥10 澄清问题（≥3 P0）→ ≥3 本质方案 → MVP 验证 → 取舍代价 → 最终建议，并更新 docs 下 8 份文件。 |
| **建议** | AI 完整读取参考后提出三条本质路线：A 受控多模态 + 人工确认 + 分类型核验，B OCR/规则优先，C 端到端多模态 LLM；使用官方 MiMo、FastAPI、pgvector、PostgreSQL 资料评分，并建议 A 4.40 分。 |
| **人工判断** | **采纳结构和推荐作为待确认提案，但不接受“自动进入实现”**。我要求旧决策和新方案都留痕；AI 据此将 V3 标记被新需求替代，把 V4 方案 A 标为“待用户接受”，真实模型与测试结果保持待执行。 |
| **验证** | 已生成/更新 8 份核心文档；澄清问题 15 个、P0 4 个、顶层方案 3 个；`cases-and-results.md` 明确区分静态检查与未运行测试。 |

### 记录 25：确认全部澄清答案并接受方案 A 🟢

| 字段 | 内容 |
|---|---|
| **目的** | 关闭身份证件与多模态方案中的待确认项，使 PRD、方案、ADR 和详细设计拥有同一组可执行约束。 |
| **输入** | 我确认：身份证件 MVP 仅居民身份证；失主图片不做多模态；候选展示掩码；拾得者逐位确认号码；同账号同候选最多 2 次；重复号码记录转管理员；其他物品隐藏描述生成 2～3 个问题，失败存草稿；优先 MiMo-V2.5；应用层 HMAC-SHA256；原图保留到记录关闭；管理员默认只看掩码和事件，明确授权才临时看原图。并要求所有待确认项改为已确认、生成详细设计。 |
| **建议** | AI 建议把确认拆成四类设计约束：发布硬门槛、确定性核验、安全与权限、失败路由；将方案 A 改为已接受，B/C 改为已拒绝；保留模型 spike 和运行测试为待执行，避免把决策确认等同于技术验证。 |
| **人工判断** | **采纳方案 A 并确认全部澄清答案，同时保留验证门槛。** 我接受拾得者逐位确认和每候选 2 次限制以降低识别错误与枚举风险；接受重复号码转管理员，避免同一证件多记录时自动放行；接受完整号码不进 LLM/日志，管理员也默认不可见。失主图片只做 PRIVATE 材料，不扩大多模态范围。 |
| **验证** | 已将 `clarifying-questions.md` 的 15 项标为已确认，并同步 PRD、方案选项、取舍矩阵、拒绝记录、最终建议和 ADR。详细设计把这些答案映射为接口、状态机、表结构和测试断言；代码与真实模型验证仍未执行。 |

### 记录 26：按任务角色拆分前端并生成三张原型 🟢

| 字段 | 内容 |
|---|---|
| **目的** | 让前端能够分别演示失主、拾得者和管理员的完整任务，同时保持账号角色、权限和敏感数据边界清楚。 |
| **输入** | 我要求在详细设计中补充前端方案，分别生成“我丢失了物品”“我捡到了物品”原型，并为管理员提供单独页面和完整设计细节。 |
| **建议** | AI 比较了三个独立前端、一个动态长表单、普通用户双任务入口 + 独立管理员布局三种方式，建议第三种：普通用户共用账号和记录中心，两条任务采用独立向导；管理员使用 `/admin` Layout。并建议前端明确路由、组件、服务端状态、敏感表单清理、失败状态、可访问性和 E2E。 |
| **人工判断** | **采纳双任务入口和管理员独立页面。** “我丢失了物品”和“我捡到了物品”是普通用户的当前任务，不是永久账号角色；这样既符合一个用户可同时发布失物和招领的需求，也避免三个应用重复开发。管理员页面必须独立，且不能靠前端隐藏实现 PRIVATE 权限。 |
| **验证** | `end-to-end-system-design.md` 已补充 4.1～4.15 前端设计，并生成失主、拾得者、管理员三张 SVG 原型；设计包含路由守卫、PUBLIC DTO、逐位确认、脱敏预览、临时原图授权和 Playwright 用例。当前只完成设计与静态检查，前端代码和浏览器 E2E 尚未执行。 |

### 记录 27：用角色—接口泳道重构总体架构图 🟡

| 字段 | 内容 |
|---|---|
| **目的** | 让总体架构图本身能够回答失主、拾得者、管理员分别可以调用哪些接口，以及接口属于哪个后端模块。 |
| **输入** | 我要求重构总体架构图：三种角色需要清晰识别各自对应接口；接口层必须有模块划分，不同模块下列出对应接口。 |
| **建议** | AI 指出现有图把三个角色合并为 React Web 后统一连接服务，无法表达接口权限。建议改成四层泳道：角色与页面层 → M1～M6 接口模块 → 领域服务层 → 数据/AI 层；认证、交接等共享模块拆成单个接口节点，使管理员不连接注册、失主不连接交接完成。 |
| **人工判断** | **采纳并进一步要求图内精确到共享模块的单接口连接。** 只把角色连到一个含多条接口的模块仍可能误解为拥有模块内全部权限，因此认证和交接模块分别拆分注册/登录/刷新、联系方式/交接完成/时间线接口。领域服务继续保持内部模块，不允许浏览器直接调用。 |
| **验证** | `end-to-end-system-design.md` V1.2 已重构 Mermaid 总体架构，包含 3 个角色、6 个接口模块、10 个领域服务及数据/AI 层；接口路径与第 12 章交叉检查，并新增 `GET /api/admin/audit-events`。当前属于设计验证，接口尚未实现。 |

### 记录 28：确认 Design Review 并把设计转换为 TDD 开发计划 🟢

| 字段 | 内容 |
|---|---|
| **目的** | 关闭详细设计阶段门，把已确认的接口、状态机、安全边界和前端原型转换成两天内可执行的开发规范与任务依赖。 |
| **输入** | 我明确“Design Review，全部确认”，要求编写 `dev.md` 并拆分生成 `task.md`；既有基线为 PRD V0.8、方案 A 和端到端详细设计 V1.3。 |
| **建议** | AI 建议冻结 V1.3，不在开发阶段重新引入待确认项；用 `dev.md` 固定依赖、目录、数据迁移、模块规则、TDD、质量门和证据规范；用 `task.md` 按 T00～T19 拆成工程、数据库、安全规则、六类接口模块、三角色前端、E2E、失败验证和交付，并为每项写明文件、Red/Green、命令、依赖和验收。 |
| **人工判断** | **采纳“设计冻结 + TDD 分层拆分”，但不把计划当作实现结果。** 我确认详细设计全部通过，允许进入开发；任务必须优先保证身份证 HMAC、2 次限制、重复号码转人工、四级数据隔离和状态机，不因两天时间降级这些安全规则。真实成员姓名和测试 PASS 只能在实际发生后填写。 |
| **验证** | `end-to-end-system-design.md` 的 14 项 Review 检查表已勾选并写明确认语义；已生成 `dev.md` V1.0 和 `task.md` V1.0，任务覆盖 26 个 REST API、三类前端视图、四组 E2E seed 与至少 8 个失败/边界场景。当前只完成文档结构与一致性校验，代码和运行测试仍未执行。 |

### 记录 29：否决全量简化，恢复详细基线后最小化重构 🔴

| 字段 | 内容 |
|---|---|
| **目的** | 在不丢失已确认的安全、状态机、测试和证据细节的前提下，把系统业务收敛为清楚的三个角色、两类物品和一条核心闭环。 |
| **输入** | 我明确要求继续采用四级权限矩阵；此前 PRD、端到端设计、dev 和 task 已被整体简化，我要求先恢复之前的详细版本，再只修改功能模块混乱和复杂的部分。 |
| **建议** | AI 检查 Git 历史并找到提交 `7e1b525` 中的 PRD V0.8、设计 V1.3、dev/task V1.0，建议以该版本为恢复基线；保留六个核心业务模块和 T00～T19，只增量增加 `UNMATCHED`、`CLAIM_REVIEW`，并让候选分只负责 Top 5、身份证继续由服务端 HMAC 精确核验。 |
| **人工判断** | **拒绝 AI 之前生成的 V2.0/12 Tasks 全量重写。** “简单”应该体现在主流程和职责清楚，而不是删除权限、安全和测试证据。我采纳恢复基线与小版本增量；保留 PUBLIC/MATCH_ONLY/VERIFICATION/PRIVATE 四级矩阵、身份证每账号每候选 2 次、重复记录转管理员和 T00～T19。普通物品不再把候选分与隐藏问答组合成“综合认领可信度”，因为公开相似度不能证明所有权。 |
| **验证** | 四份主文档已恢复后分别更新为 PRD V0.9、设计 V1.4、dev/task V1.1；通过 Git 差异确认是增量修改而非大段删减，并检查四级矩阵、任务编号、接口集合、Markdown 代码围栏和旧 V2.0 状态。当前仅完成文档静态验证，代码、模型 spike 和运行测试尚未执行。 |

### 记录 30：否决过度复杂的架构分层，改为角色化四层功能图 🔴

| 字段 | 内容 |
|---|---|
| **目的** | 让评委可以在一张图中快速识别失主、拾得者和管理员的核心功能，以及 AI、安全和系统能力如何提供支撑。 |
| **输入** | 我指出 AI 给出的模块树仍然太复杂，并提供最终清单：核心功能层、AI能力层、安全与防冒领层、系统支撑层；核心层只按用户管理、拾得者、失主和管理员组织，要求参考上传图片生成对齐、均分且不溢出的 SVG。 |
| **建议** | AI 原先建议增加角色主线、AI、安全、数据流程和基础服务等更多层级；在收到我的明确清单后，改为四层横向架构，核心层用三个角色面板和一个共享用户管理条，其他三层各分三个能力组，并沿用参考图的彩色大层、白色包裹面板和同色能力块。 |
| **人工判断** | **拒绝额外的“数据与流程层”“治理层”“基础服务层”和接口级节点。** 这些细节在详细设计正文已经存在，继续放进总图只会增加理解成本。我确认以四层清单为唯一图示范围；详细接口、服务和数据流不删除，但不在这张功能架构图中展示。 |
| **验证** | SVG 已通过 Chrome 以 1600×1410 渲染并人工检查：四层包裹关系清楚，三列面板等宽对齐，文字未越出外层容器；详细设计 4.1 已替换为该图。当前验证仅针对图形布局和文档引用，不代表系统功能已实现。 |

---

## 本阶段人工判断汇总（阶段七～九）

| 类型 | 数量 | 代表性内容 |
|---|---:|---|
| 🔴 拒绝 / 替代 | 4 | 拒绝 Jinja2 SSR 选 React；拒绝 OpenAI 模型选用户指定模型；拒绝手动设置问题改为 LLM 生成；要求 AI 做一致性验证 |
| 🟡 修改 AI 方案 | 6 | 认证随前端改为 JWT；匹配流程补充双方向量化+TOP-5；PRD 原地升级；旧图像边界改为受控多模态；证件认领改为待交接 |
| 🟢 附条件采纳 | 2 | 文档结构和决策链路框架；V4 三方案推荐仅作为待确认提案 |

### 已形成的明确拒绝/修改证据（累计）

1. 我曾拒绝图片视觉识别和图片向量匹配路线；阶段九因新需求将其修改为“受控多模态发布草稿”，仍拒绝视觉相似度直接认领。
2. 我替代了先前的 MySQL 决策，改用 PostgreSQL + pgvector。（阶段四）
3. 我修改了”高置信度直接确认”的原始想法：系统只能自动进入待交接。（阶段二）
4. 我修改了原 25 分外观 + 25 分语义的重复评分，合并为 50 分。（阶段五）
5. 我拒绝了 AI 推荐的 Jinja2 SSR，选择 React 18 + Vite。（阶段七）
6. 我拒绝了 AI 推荐的 OpenAI 模型，选择 text-embedding-v4 + mimoV2.5-pro。（阶段七）
7. 我拒绝了 PRD 原设计的”拾得者手动设置隐藏问题”，改为 LLM 根据隐藏特征自动生成问题。（阶段七）
8. 我拒绝了 AI 的”已更新完毕”声明，要求全局一致性扫描，发现并修复 18 处残留旧术语。（阶段八）

### 当前验证边界

- 已验证：V0.8/V4 文档结构、15 个澄清问题与 4 个 PO、3 条本质方案、证据化评分、双流程与安全边界已经落盘。
- 尚未验证：多模态名称/特征/证件号码提取效果、图片自动脱敏、HMAC 实现、text-embedding-v4 匹配效果、mimoV2.5-pro 核验质量、React + JWT 联调、接口权限和全部运行测试。

---

## 追加记录 31：个人贡献归属更正与证据审查 🟡

| 字段 | 内容 |
|---|---|
| **目的** | 根据团队成员真实贡献，纠正早期个人 AI 日志中过度归属于“我”的表述，并从当前仓库整理个人贡献证据。 |
| **输入** | 我确认：四级数据分类由徐胜宇提出；图片识别想法由黄孝梁提出，执行和决策由我与徐胜宇共同完成；角色功能任务和身份证件分流由我提出并决策；其余产品/架构判断主要由我思考决策；PRD、方案、设计、dev 和过程文档由我记录；前端由我先设计 UI 再独立实现。输入证据还包括 Git 作者、主文档、过程日志、`prototype/ui`、`frontend` 和 T13～T16 开发记录。 |
| **建议** | AI 建议按“想法提出者 / 决策者 / 工程落地者 / 文档记录者 / 代码实现者”拆分贡献；重点说明我的后端架构思路、PRD 迭代、文档工程和前端闭环，同时明确未实现后端代码、真实模型和端到端联调的边界。 |
| **人工判断** | **采纳分层归属并更正记录 8。** 记录 8 中“我提出四级模型”的表述不准确，四级分类的原创与提出归徐胜宇；我的贡献是将其工程化写入 PRD、设计、DTO、AI 输入、日志和测试。图片识别不归为我个人原创，方案执行与决策属于我和徐胜宇共同贡献。其余贡献按本轮确认记录。 |
| **验证** | 新增 `docs/reflection/szy-personal-contribution.md`，并在 `individual-contributions.md` 追加个人摘要；重新执行前端 `npm.cmd test`，6 个文件、46 个测试通过；执行 `npm.cmd run build`，TypeScript 与 Vite 构建通过、155 个模块转换成功。当前 `frontend/` 尚未 Git 跟踪，后端仍只有架构与开发规划证据。 |

> 归属更正说明：记录 8 原文作为真实历史保留，不删除、不覆盖；从本条开始，以“徐胜宇提出四级分类，宋子仪负责工程化落地和文档化”为准。
- 后续规则：只有实际执行并保存结果后，才能把”计划验证”更新为”验证通过”。

---

## 追加记录 32：按系统功能架构图生成项目汇报话术 🟢

| 字段 | 内容 |
|---|---|
| **目的** | 将 PRD 和详细设计中的大量内容压缩成一条能配合系统功能架构图讲清楚的项目汇报主线。 |
| **输入** | `docs/design/system-function-architecture.svg`、`prd.md`、`docs/design/end-to-end-system-design.md`，以及当前前端实现与测试事实。用户要求按架构图展开，并把个人贡献相关材料集中到 `szy/`。 |
| **建议** | AI 建议按“核心功能层 → AI 能力层 → 安全与防冒领层 → 系统支撑层 → 一条端到端示例 → 当前完成情况与边界”展开；每层分别回答角色职责、AI 边界、安全规则和技术落地，避免按 PRD 章节顺序逐条念文档。 |
| **人工判断** | **采纳按架构图自上而下的叙事方式。** 汇报重点不是罗列接口，而是解释系统为什么把候选匹配、认领核验和真实交接拆开；同时必须披露前端已实现、后端仍处于详细设计阶段，不能把 Mock 页面表述为完整 E2E。四份原始过程文档继续保留，`szy/` 中只保存个人答辩使用的副本，避免破坏原目录引用和追加式证据链。 |
| **验证** | 新增 `szy/project-presentation-script.md`，包含 8～10 分钟完整话术、指图提示和 30 秒精简版；内容逐层覆盖架构图全部模块，并与 PRD 的双类型核验、Top 5、四级权限、状态机和设计文档的真实完成边界交叉核对。 |
