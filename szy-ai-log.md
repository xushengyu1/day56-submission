# AI 协作日志 — SZY — AI 失物招领匹配与认领复核系统

> 本文件是我的个人 AI 协作记录，只筛选实际改变问题判断、产品边界、AI 边界、评分规则或阶段结论的对话。
> 每条记录严格包含：目的 / 输入 / 建议 / 人工判断 / 验证。
> **关键决策标记**：🔴 拒绝 AI 建议 / 🟡 修改 AI 建议 / 🟢 采纳但有附加条件。
> 当前状态：PRD V0.6 已确认；代码、单元测试、集成测试和端到端测试尚未开始，文中不得把计划验证写成实际通过。

---

## 阶段一：作业理解与交付范围

### 记录 1：采用阶段门推进并保留全过程证据 🟢

| 字段 | 内容 |
|---|---|
| **目的** | 确定两天作业的实施流程以及过程文档如何留存。 |
| **输入** | 作业题目截图；我提出的完整流程：问题拆解 → PRD → PRD 复盘确认 → design_option → 方案确认 → design → Design Review → dev → TDD → 实现与测试 → 证据链 → 答辩；指定参考 `day5材料` 中的文件模板。 |
| **建议** | AI 建议不要直接编码，先按阶段门生成问题诊断、PRD、方案、设计、开发和验证材料；同时把每次澄清、采纳、修改和拒绝写入对应过程文档。 |
| **人工判断** | **采纳但增加硬约束**：我同意阶段门方式，但要求最终文件结构严格参考 `E:\A-xiaomi\xiaomi\2\day5\day5材料`，所有实际交互都必须留下记录，不能只生成结果文档，也不能事后伪造 AI 日志。 |
| **验证** | `day6` 已按模板建立诊断、协作、AI、方案和验证文档；当前阶段状态写入 `README.md`、`docs/collaboration/decision-log.md` 和 `docs/diagnosis/prd-iteration-log.md`。 |

### 记录 2：把交付重点从“功能数量”收缩为最小证据闭环 🟡

| 字段 | 内容 |
|---|---|
| **目的** | 明确两天内真正需要证明什么，避免做成大而全的失物招领平台。 |
| **输入** | 我补充的六项评分重点：问题判断、方案取舍、AI 边界、MVP 可验证、证据链可信、答辩能抗追问；以及双技术端、五类以上 AI 协作、失败场景、端到端测试等硬性要求。 |
| **建议** | AI 初版 PRD 包含较多页面、状态和外围能力；Review 后建议缩成一条“发布 → 候选 → 认领核验 → 自动或人工路由 → 交接 → 留痕”的完整主线。 |
| **人工判断** | **修改 AI 的初版范围**：我确认评分重点不是页面数量和视觉效果，而是一个可以运行、触发判断、展示失败并追溯证据的最小系统。复杂申诉、多组织、多渠道通知和生产级能力均不进入 MVP。 |
| **验证** | `prd.md` 第 4 章已写成 Goals / Non-Goals；第 6 章只保留核心主流程；`README.md` 明确真实实现、模拟数据、当前边界和未实现内容。运行结果仍需开发完成后验证。 |

---

## 阶段二：角色、保管责任与认领边界

### 记录 3：重新定义业务用户、管理员和实物保管责任 🟡

| 字段 | 内容 |
|---|---|
| **目的** | 消除 AI、管理员、失物招领处和实物保管人之间的角色混淆。 |
| **输入** | 我明确：业务用户只分失主和拾得者；AI 不是角色；不设置失物招领处；实物由拾得者保管；异常申请由统一管理员审核。 |
| **建议** | AI 建议把“业务用户”和“系统权限角色”分层：失主、拾得者是业务用户，管理员是审核角色，AI 是系统能力；管理员只检查系统证据，不接触和保管实物。 |
| **人工判断** | **采纳分层表达并修正初版角色模型**：我接受管理员作为审核权限角色，但拒绝把 AI、保管人或失物招领处写成独立用户角色。拾得者必须承担保管和最终交接确认责任。 |
| **验证** | `prd.md` 第 2、3、6 章分别给出用户画像、权限边界和三角色流程；流程中只有拾得者可以在线下交接后将物品标记为“已认领”。实现后需用越权测试验证。 |

### 记录 4：隐藏问题必须由拾得者手工设置 🟡

| 字段 | 内容 |
|---|---|
| **目的** | 明确隐藏核验信息的来源，以及拾得者未提供隐藏信息时如何处理。 |
| **输入** | 初版存在“AI 生成隐藏问题”的表述；我指出只有拾得者持有实物，失主信息只应用于初步候选匹配，并追问拾得者未填写隐藏信息怎么办。 |
| **建议** | AI 建议由拾得者手工填写问题和标准答案，AI 只提示可检查的部位、检查问题质量和答案泄露风险；若没有核验基准，不允许正式发布。AI 进一步建议至少设置 2 题，其中至少 1 题为关键问题。 |
| **人工判断** | **修改“AI 自动生成问题”的建议并采纳安全门槛**：我确认隐藏问题只能由拾得者根据实物手工设置，AI 不能编造标准答案。接受“至少两题且一题关键”的 MVP 门槛；不满足时只能保存草稿。 |
| **验证** | `prd.md` 7.3、7.6 和主流程 Mermaid 已包含草稿门槛、手工问答和 AI 质量检查。开发后需测试 0 题、1 题、无关键题和问题泄露答案四类失败场景。 |

### 记录 5：高可信只允许进入“待交接”，不能直接确认归属 🟡

| 字段 | 内容 |
|---|---|
| **目的** | 定义高置信度认领如何自动流转，以及管理员何时介入。 |
| **输入** | 我提出高置信度可以直接确认，只有需要复核的申请交给管理员；交接后由拾得者修改为“已认领”。 |
| **建议** | AI 指出公开候选高分只能证明两条记录相似，不能证明所有权；建议拆成候选匹配分、隐藏核验分和综合认领可信度，三者同时过线且无风险时仅自动进入“待交接”，而不是直接标记“已认领”。 |
| **人工判断** | **接受 AI 对我原方案的安全性修改**：自动流转条件定为候选分 ≥80、隐藏核验分 ≥85、综合可信度 ≥85、至少两题且关键题无冲突、无 AI 异常或试探风险；其它情况转管理员，不由 AI 自动拒绝。实际交接后仍只允许拾得者确认“已认领”。 |
| **验证** | `prd.md` 7.7.1 写明全部条件，8 章定义状态流转，13 章列出正向与反向验收场景。阈值只是 Demo 初始规则，仍需边界样例验证误放行和误转人工情况。 |

---

## 阶段三：AI 使用边界与产品表达

### 记录 6：拒绝使用图像识别和图片 AI 匹配 🔴

| 字段 | 内容 |
|---|---|
| **目的** | 决定拾得者上传整体图片后，是否使用视觉模型提取类别、颜色和物品特征。 |
| **输入** | 我分析了图像识别限制：同类黑伞外观接近、角度和光照影响大、小划痕或内部标记不可见、模型可能生成不存在的细节，而且公开图片可能泄露隐藏答案。 |
| **建议** | AI 初版架构曾包含根据图片提取特征或辅助匹配的可能；Review 后建议保留整体图片上传，但只供失主人工浏览，不进入任何 AI 评分。 |
| **人工判断** | **拒绝视觉 AI 路线**：我决定图片只作为人工发现候选的辅助材料，不调用图像识别、不生成图片向量、不提取隐藏特征，也不进入候选分或认领可信度。上传时必须提示拾得者遮挡敏感内容和隐藏答案。 |
| **验证** | `prd.md` 4.2 Non-Goals、5.4 图片分级、7.3 图片需求和 7.4.1 评分规则均排除图片 AI；后续需要检查接口、数据库和提示词中不存在图片特征或图片向量。 |

### 记录 7：从参考文字中只提取适合本项目的产品描述和画像 🟡

| 字段 | 内容 |
|---|---|
| **目的** | 补齐产品背景、解决的问题、核心价值、架构图、技术栈和用户画像。 |
| **输入** | 我上传的产品描述参考文字，并明确后端采用 Python + FastAPI、当时数据库采用 MySQL；要求只提取需要的部分。 |
| **建议** | AI 建议复用参考材料中“信息分散、描述差异、记忆偏差、防冒领、异常分流、证据追溯”等内容，同时生成需求级架构图和失主、拾得者、管理员画像；不照搬其中的 SQLite、图像识别、管理员保管实物等设定。 |
| **人工判断** | **采纳提炼方式但拒绝照搬参考方案**：我保留能解释当前 MVP 的背景和用户痛点，删除与已确认边界冲突的视觉 AI、失物招领处、AI 自动生成隐藏事实等内容。前端、ORM、认证和具体模型不在 PRD 阶段提前锁定。 |
| **验证** | `prd.md` 第 1 章包含产品描述、Mermaid 架构和技术栈，第 2 章包含三类画像；`docs/diagnosis/prd-iteration-log.md` 迭代 02 记录了采纳与拒绝内容。 |

---

## 阶段四：数据权限、数据库与流程表达

### 记录 8：把公开/隐藏两级改为四级数据权限模型 🟡

| 字段 | 内容 |
|---|---|
| **目的** | 解决联系方式被放入“公开信息”但又要求授权后显示的权限矛盾。 |
| **输入** | 我提出 `PUBLIC / MATCH_ONLY / VERIFICATION / PRIVATE` 四级含义、候选页/匹配服务/管理员的访问矩阵，以及地点、图片和联系方式的跨级示例。 |
| **建议** | AI 建议把四级定义转成可测试的服务端访问规则：候选页只返回 PUBLIC；匹配使用 PUBLIC + MATCH_ONLY；核验服务使用 VERIFICATION 但不回传答案；管理员按需查看前三类；PRIVATE 默认脱敏且不进入 AI。 |
| **人工判断** | **采纳四级模型并进一步限制 PRIVATE**：我确认联系方式和原始凭证不属于 PUBLIC，管理员也不能因为角色身份默认查看全部 PRIVATE。字段级别由字段值、用途和生命周期决定，而不是由字段名称固定决定。 |
| **验证** | `prd.md` 第 5 章已经包含权限矩阵、四级定义、同字段分级示例和访问规则。后续 TDD 必须覆盖普通用户响应字段扫描、管理员脱敏和联系方式授权前越权访问。 |

### 记录 9：把 MVP 范围前置为 Goals / Non-Goals 🟢

| 字段 | 内容 |
|---|---|
| **目的** | 避免产品目标和后置 MVP 范围重复，导致同一需求在两个章节中发生漂移。 |
| **输入** | 我提供了 Goals / Non-Goals 的参考格式，并要求写入 PRD 第 4 章、删除原第 12 章 MVP 范围、重新调整后续编号。 |
| **建议** | AI 建议合并重复范围，形成可验证的目标和明确的非目标，并将角色与权限放在第 3 章、Goals / Non-Goals 放在第 4 章。 |
| **人工判断** | **采纳结构调整但要求内容以本项目事实为准**：不照抄参考项目的 CLI、FAQ 和 mock LLM 内容，而是改写为本失物招领系统的双技术端、核心主线、失败场景、数据权限和 AI 边界。 |
| **验证** | `prd.md` 第 4 章包含 12 项 Goals、10 项 Non-Goals 和产品原则；原独立 MVP 范围已删除，顶层章节连续编号为 1～15。 |

### 记录 10：用 PostgreSQL + pgvector 替代 MySQL 🔴

| 字段 | 内容 |
|---|---|
| **目的** | 统一存储结构化业务数据和文本向量，降低两天 MVP 的服务数量。 |
| **输入** | 此前 PRD 已按我的旧决定写为 MySQL；随后我明确数据库改用 PostgreSQL，并要求同时保存结构化数据和向量数据。 |
| **建议** | AI 建议使用 PostgreSQL 常规表或 JSON 保存业务、状态和审计数据，启用 pgvector 保存公开文本 embedding；不额外部署 Milvus、Pinecone 等独立向量数据库。 |
| **人工判断** | **拒绝并替代此前 MySQL 决策**：我确认采用 PostgreSQL + pgvector。只保存 `PUBLIC + MATCH_ONLY` 文本向量；图片、VERIFICATION 和 PRIVATE 数据不进入候选向量。embedding 模型、维度、距离和索引留到方案阶段比较。 |
| **验证** | `prd.md` 架构、技术栈、数据对象和非目标已统一为 PostgreSQL + pgvector；历史 MySQL 记录保留并标注被替代。后续需要验证扩展初始化、向量维度、查询失败降级和权限隔离。 |

### 记录 11：核心业务流程统一改为 Mermaid 🟢

| 字段 | 内容 |
|---|---|
| **目的** | 让自动流转、管理员复核和三角色责任在评审与答辩中更直观。 |
| **输入** | 我要求把 `prd.md` 第 6 章中的流程图全部转换为 Mermaid。 |
| **建议** | AI 建议分别保留主流程、失主流程、拾得者流程和管理员流程四张图，在主流程中明确隐藏问答发布门槛、高可信分支、人工分支、交接和审计终点。 |
| **人工判断** | **采纳四图结构**：我没有把三个角色压缩成一张大图，因为分图更容易说明个人操作和管理员边界；同时要求业务语义保持不变。 |
| **验证** | `prd.md` 6.1～6.4 已包含 4 个 Mermaid 代码块，没有残留 text 流程图；已做源码结构检查。当前环境没有 Mermaid CLI，实际渲染仍需在后续文档构建或演示环境验证。 |

---

## 阶段五：候选评分复盘与 PRD 确认

### 记录 12：合并重复的外观分和公开描述语义分 🟡

| 字段 | 内容 |
|---|---|
| **目的** | 消除候选匹配标准对颜色、品牌和公开外观的重复计分。 |
| **输入** | PRD V0.5 原评分为：颜色与公开外观 25、时间 20、地点 20、公开描述语义 25、完整度 10；我指出颜色和公开外观本质上已经属于公开描述语义。 |
| **建议** | AI 建议删除独立外观维度，把公开描述语义提高为 50 分，保留时间 20、地点 20、完整度 10，总分仍为 100；类别继续作为硬门槛。向量相似度由 AI embedding + pgvector 实现。 |
| **人工判断** | **采纳合并方案并限定只计算一次证据**：颜色、品牌、名称和其它公开外观可以写入公开描述并参与向量语义匹配，但不得再单独加分；图片和隐藏信息仍不进入向量。 |
| **验证** | `prd.md` 7.4.1 已改为 50 + 20 + 20 + 10，字段说明、目标、测试计划和过程日志同步更新。后续必须验证“语义很像但时间或地点明显冲突”不会被 50 分语义权重错误放行。 |

### 记录 13：确认 PRD V0.6 为需求基线 🟢

| 字段 | 内容 |
|---|---|
| **目的** | 结束 PRD Review，建立后续方案设计可以依赖的稳定需求基线。 |
| **输入** | 完成角色、隐藏核验、图片边界、四级权限、PostgreSQL + pgvector、Mermaid 流程和候选评分五轮迭代后的 `prd.md` V0.6。 |
| **建议** | AI 建议把当前版本标记为已确认基线；确认动作本身不增加版本号，只有后续需求内容发生变化时才从 V0.7 起新增迭代；下一阶段进入 `design_option.md`，不能跳过方案比较直接编码。 |
| **人工判断** | **采纳版本治理建议**：我明确当前 PRD 已确认，后续如果有变更再迭代。V0.6 保留为已确认事实，不允许静默覆盖；方案、设计和实现仍按既定阶段门推进。 |
| **验证** | `prd.md` 头部已标记“V0.6 / 已确认（2026-07-16）”；确认事实同步到 `docs/validation/review-record.md`、`docs/collaboration/decision-log.md`、会议记录和 PRD 迭代记录。 |

---

## 阶段六：个人 AI 证据整理

### 记录 14：个人 AI 日志只保留完整决策链 🟢

| 字段 | 内容 |
|---|---|
| **目的** | 把本次 PRD 阶段中真正有判断价值的 AI 协作整理成我的个人贡献证据。 |
| **输入** | 我指定参考 `xiaomi/new-day3/ai-log.md`，并要求文件名为 `szy-ai-log.md`；每条必须包含目的、输入、建议、人工判断、验证，其中人工判断必须明确采纳、修改或拒绝了什么以及原因。 |
| **建议** | AI 建议按参考文件的阶段结构和红黄绿标记整理，只筛选改变产品范围、角色责任、AI 边界、数据权限、技术事实、评分规则或阶段状态的记录；普通文件操作不用于凑数，未执行测试不写成通过。 |
| **人工判断** | **采纳筛选与五字段格式，并强化真实性要求**：我不需要原始对话的机械转录，而需要能说明个人判断的决策链。所有记录以第一人称写清我的取舍；“验证”必须区分已存在的文档证据和未来需要执行的系统测试。 |
| **验证** | `szy-ai-log.md` 已生成；使用脚本检查每条记录的五字段数量一致。文件末尾另外列出拒绝/修改证据和当前验证边界，便于答辩追溯。 |

---

## 本阶段人工判断汇总

| 类型 | 数量 | 代表性内容 |
|---|---:|---|
| 🔴 拒绝 / 替代 | 2 | 拒绝图像识别与图片 AI 匹配；用 PostgreSQL + pgvector 替代 MySQL |
| 🟡 修改 AI 或原始方案 | 7 | 收缩 MVP、修正角色、手工隐藏问答、三分数安全门槛、参考内容筛选、四级权限、评分去重 |
| 🟢 附条件采纳 | 5 | 阶段门、Goals/Non-Goals、Mermaid 四图、PRD 基线治理、个人 AI 日志五字段规则 |

### 已形成的两条明确拒绝/明显修改证据

1. 我拒绝了图片视觉识别和图片向量匹配路线，整体图片只供人工查看。
2. 我替代了先前的 MySQL 决策，改用 PostgreSQL + pgvector，并限制只有公开文本可以进入候选向量。
3. 我修改了“高置信度直接确认”的原始想法：系统只能自动进入待交接，不能直接确认归属或已交接。
4. 我修改了原 25 分外观 + 25 分语义的重复评分，合并为公开描述向量语义 50 分。

### 当前验证边界

- 已验证：文档结构、章节编号、决策同步、Mermaid 源码块数量、当前技术事实一致性。
- 尚未验证：具体 embedding 模型效果、评分阈值有效性、Mermaid 实际渲染、接口权限、运行主流程、失败场景、单元/集成/端到端测试。
- 后续规则：只有实际执行并保存结果后，才能把“计划验证”更新为“验证通过”。
